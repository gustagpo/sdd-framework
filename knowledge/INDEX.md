# Knowledge Index — SDD Framework

> Índice da base de conhecimento acumulada entre projetos. **1 linha por lição**, com tags `[stack][papel][passo]`. Os agentes leem ESTE arquivo primeiro e abrem só as lições relevantes. Mantido pelo Team Leader na retrospectiva (Passo 7) de cada rodada — atualizar em vez de duplicar; lição errada é removida; lição que contradiz um standard corrige o standard.

## Processo (PROCESS.md)

- P-001 [processo][telemetria] Custo por agente exige label determinístico na invocação + telemetria em 1 comando por passo — **limitação 19/08**: rodada inteira com tokens `unavailable` (0 eventos resolvidos, sem transcript) ⇒ custo inauditável se registra como lacuna, nunca se estima; **reforço 21/08**: 22 `agent_run` sem `label` ⇒ `— / —` em toda a rodada — label é DERIVADO pelo coletor de `(run_id,step,agent,iter)`, nunca entrada de prompt; `sdd-report` avisa "N sem label" em vez de imprimir `—`
- P-002 [processo][orquestrador] Uma feature por sessão — 2 rodadas na mesma sessão estouram o contexto
- P-003 [processo][gates] Gates consomem DIGEST (≤30 linhas) do agente, nunca o documento inteiro — **e o digest do agente é atalho, não fonte: sem ele, derivar do artefato (P-023)**
- P-004 [processo][telemetria] agentType do transcript tem 2 formatos (name livre | subagent_type namespaced) + ruído a filtrar
- P-005 [processo][telemetria] Só registre agent_run se houve invocação real de subagent — trabalho inline não tem transcript
- P-006 [processo][orquestrador] Spawn paralelo (tmux) esgota slots com agentes idle acumulados — limpar slots concluídos entre fases é pré-condição do próximo spawn
- P-007 [processo][gates] Gate supervised reverte propostas: digest leva a proposta E a alternativa descartada com custos — corte de escopo é o mais revertido — **reforço 21/08**: recomendação conservadora (não tocar o motor de ciclo) revertida; segura porque o digest levou junto util puro + paridade byte-a-byte + dry-run em produção
- P-008 [processo][qa][passo-6] Baseline de regressão (unit E e2e) se mede no HEAD pré-feature via `git stash -u`, nunca se herda do TESTS.md/documento — senão falha pré-existente conta como regressão
- P-009 [processo][dev-backend][passo-3] O consolidador do CONTRACT deve contradizer o próprio draft quando outro papel traz evidência melhor (a SPEC dizia "sem migration"; o DevOps mostrou migration de dados obrigatória); **reforço 12/08**: 5 inversões numa rodada — a decisiva veio do Security ABRIR o código (P-024 no Passo 3), não de argumento
- P-010 [processo][sdd-init][team-leader] Dependência no package.json não prova uso — convenção só entra em doc com evidência de import (grep); dep órfã vira débito
- P-011 [processo][qa][gates] Infra de teste ausente: detectar no baseline, decidir estratégia (híbrida) no gate, catalogar os casos "requer infra" e listar o não-verificado no EVALUATION
- P-012 [processo][ops][security][passo-6] Feature que gasta dinheiro exige avaliadores DevOps/Security independentes além do QA — bloqueante ALTA (guarda fail-open de compra) veio do raciocínio operacional com a suíte funcional 100% verde; a correção inclui o teste que faltava (dado hostil), não só o fix; **reforço 01/08**: convergência independente de 2 papéis no MESMO achado = sinal de confiança mais forte da avaliação; revalidação do loop se faz por REPRODUÇÃO independente, nunca por relato de quem corrigiu; **reforço 25/08**: avaliador com FERRAMENTA diferente (boot do `dist` × suíte) achou o único bloqueante (API não subia com build + 10 340 testes verdes) — gate de deployability roda no artefato, por quem não escreveu, e a revalidação o reproduz · **reforço 27/08**: D-1 🔴 (migration semeando coluna de migration PENDENTE ⇒ `42703` + rollback total) veio SÓ da deployability lendo o dump — "existe no ALVO?" é pergunta operacional
- P-013 [processo][team-leader][passo-1] Reconciliar o pedido com o código no discovery — parte pode já existir de rodada anterior; RESEARCH abre com "Escopo real" (evidência arquivo:linha) e a spec cobre só o delta; **reforço 19/08**: reconciliar inclui procurar decisões CONTRÁRIAS de rodadas intermediárias (adendo invertera a premissa central de um discovery de 16 dias) — discovery é hipótese com prazo de validade
- P-014 [processo][qa][passo-4] Teste TDD vermelho tem de falhar por ASSERÇÃO carregando — `Cannot find module` conta 0 no Jest e distorce o tamanho da bateria; criar stub do módulo junto
- P-015 [processo][orquestrador][passo-6] Spawn em lote: agente caído por limite de sessão fica pendurado horas — monitorar atividade com timeout curto (~15-20min sem tool-call = suspeito), re-spawnar limpo cedo (retry ~min vs detecção tardia ~horas); queda por limite é correlacionada no lote, checar os irmãos na hora — **evoluída em P-042**: antes de re-spawnar, retomar por mensagem; **reforço 25/08**: 3 agentes do Passo 5 pendurados 52 min sem UMA escrita em disco (155 de 571 min da rodada) — watchdog por `mtime` dos artefatos (4–8 min para implementação; 15–20 para avaliação), não só por tool-call
- P-016 [processo][orquestrador] Subagente com CWD num subrepo cria `specs/` órfão — docs de feature/STATE/LESSONS/knowledge por caminho ABSOLUTO injetado no prompt (agente nunca infere a raiz); fechamento confere path canônico + ausência de specs/ órfão nos subrepos; **reforço 19/08**: a deriva pode ser do CWD do PRÓPRIO orquestrador em sessão longa (pasta da feature nasceu em `algar-back/apps/api/src/specs/`) — path canônico resolvido 1× no Gate 0, inclusive para RUN.jsonl/DASHBOARD; fechamento confere órfão E telemetria no mesmo diretório
- P-017 [processo][telemetria] `sdd-log --type note` com run_id nulo corrompe o RUN.jsonl (flush race) — evento sem run válido é REJEITADO na origem; preferir eventos tipados a note; path do RUN.jsonl absoluto e injetado; linha inválida é quarentenada, não deletada
- P-018 [processo][qa][security][passo-3][passo-4] Invariante "fonte única" no CONTRACT nasce com spec de PARIDADE (entradas hostis × todos os call-sites); reconciliar divergência ELIMINANDO um lado (guarda mais ampla), nunca sincronizando dois predicados; **caso positivo 12/08**: matriz RBAC única + testes de paridade = 0 achados de authz em auditoria de 2 papéis; **reforço 21/08**: "reusa X" no SPEC é fonte única — cópia passou 245 verdes com whitelist asserida contra a própria constante (cliente lia "sem nota fiscal"); spec de paridade de CHAVES entre as 2 superfícies · **reforço 27/08**: regra duplicada front×back por desenho — paridade só sobre vetores de saída não cobria o RAMO (situação); paridade é por tabela de ramos, ou o backend manda o valor e o front só exibe
- P-019 [processo][qa][passo-4][passo-6] Campo opcional de resposta contratado sem teste contra a classe REAL não é entregue (ausência é silenciosa em TS) — auditoria confere campo a campo do §API; degradação devolve campo AUSENTE, nunca []/neutro; **reforço 18/08**: vale para campo de ENTRADA (P-039)
- P-020 [processo][orquestrador][gates] Allowlist de Bash estreita trava a rodada no meio do Passo 5 e o agente NÃO pode ampliá-la (anti-escalada, bloqueio correto) — conferir cobertura dos comandos da rodada no Gate 0; lacuna é pedido ao usuário
- P-021 [processo][team-leader][passo-1][passo-5][passo-6] Renomear vocabulário de produto não é find/replace: inventário âncora-a-âncora + lista MANTER; gênero em cascata; falsos amigos; tradução literal pode fabricar informação FALSA; verificação escopada ao arquivo + hash p/ par cross-repo + diff normalizado (resíduo = mudança estrutural); rename REVELA defeito pré-existente sem causá-lo (medir com o rótulo antigo)
- P-023 [processo][orquestrador][gates] O ARQUIVO é a entrega: rodada inteira em que nenhum subagente devolveu mensagem final — idle sem texto com artefato presente é SUCESSO; o digest do gate se DERIVA do arquivo por passo barato, nunca depende do relatório do agente; **reforço 04/08**: 2ª rodada consecutiva 100% sem mensagem final (20 invocações) — derivação por artefato é o caminho PADRÃO, não contingência; **reforço 12/08**: 3ª rodada, 2º projeto e outro runner — o padrão é dos agentes, não do projeto; **reforço 14/08**: evoluiu para CONVENÇÃO — o prompt manda o agente gravar o próprio digest em `drafts/DIGEST.step<N>.<papel>.md` (100% de aderência na rodada, gates consomem o arquivo direto, custo ~zero) — **reforço 4 (17/08)**: rodada inteira sustentada só por `drafts/DIGEST.step<N>.<papel>.md`; ausência do ARQUIVO (não da mensagem) = passo incompleto — **reforço 5 (19/08)**: runner engoliu 100% das mensagens finais e nada se perdeu porque, além do digest-arquivo, **cada documento termina com a seção "Resumo para o orquestrador"** (duas fontes no disco; ausência de qualquer uma = passo incompleto) · **reforço 27/08 (algar, integracao-d4sign)**: runner sem mensagem final em 27/27 invocações — `drafts/ENTREGA.<passo>.<papel>.md` desde o Passo 1 (inclusive o 7) e o orquestrador espera o ARQUIVO
- P-024 [processo][orquestrador][security][passo-6] Avaliador erra lendo código (fallback defensivo MORTO lido como caminho de produção ⇒ bug inexistente "crítico"): divergência entre papéis se arbitra ABRINDO o código e checando alcançabilidade, nunca pelo veredito mais alarmante; achado derrubado vira nota no EVALUATION; guarda morta é débito; **reforço 25/08**: consolidador REBAIXOU ✅→⚠️ um achado do Security abrindo o código (mutante morto no compilador, 0 asserção) e a própria arbitragem errou onde contou de cabeça (2 × 3 bytes) — divergência numérica se arbitra MEDINDO; rebaixamento e erro ficam como nota com evidência
- P-025 [processo][orquestrador][telemetria] `RUN.jsonl` é ponto de RETOMADA: rodada interrompida continua do 1º passo sem evento de conclusão (artefato conferido em disco) — passo concluído não se refaz, senão regenera documento já aprovado no gate
- P-022 [processo][qa][passo-3][passo-6] Lista derivável por 2 papéis se levanta 2× independentemente e compara (conjunto idêntico = confiança; divergência = erro de alguém); painel duplicado declara fonte única (vence o artefato versionado); mtime de artefato mutável não sustenta atribuição causal — verificar destrói a evidência; vale conteúdo × HEAD
- P-032 [processo][orquestrador][ops][passo-5][passo-6] Medição de gate de árvore inteira durante execução paralela produz achado falso (2 bloqueantes inventados numa rodada) — gate se mede no estado FINAL das frentes; alegação de agente sobre fronteira alheia se verifica no código antes de virar decisão/débito; função implementada e não chamada é indistinguível de ausente (guarda de fiação fecha a classe) — **reforço 21/08**: 5 rodadas no MESMO working copy ⇒ falha fora do recorte só conta se REPETIDA e com `error TS` em arquivo nosso (`git status` antes de creditar); slot de migration tomado 2× na rodada ⇒ descobrir pelo sufixo (N-045)
- P-033 [processo][ops][gates][passo-6] Reprovação no Passo 6 pode ser do ARTEFATO do avaliador (runbook/censo), não do código: findings classificam o ALVO; iteração de correção só-documento (0 código) NÃO revalida a suíte — vereditos das seções de código permanecem —, mas exige re-veredito escrito da seção + histórico no consolidado; runbook de go-live se verifica contra o código (freio para o que promete? SQL espelha o predicado filtro a filtro?) e se re-verifica após decisão de gate que muda topologia
- P-027 [processo][team-leader][qa][passo-1][passo-6] Checkbox de plano e doc de convenção são AFIRMAÇÕES a verificar (10/30 `[x]` caíram, 1 `[ ]` estava feito): tabela-mestra com veredito+evidência por critério; conferência sempre doc→código; correção que atualiza doc antes do código é regressão silenciosa · **reforço 27/08**: achado de revisão afirmou import que não existia (`TS2304` se corrigido "como descrito") — corretor reconfere `arquivo:linha`; ID pareado "B-087/088" = falso negativo de cobertura
- P-028 [processo][orquestrador][ops][passo-6] Agentes paralelos × infra local compartilhada: colisão de nome de container é artefato da concorrência, não defeito; orquestrador sobe a stack 1× antes do spawn; teardown só após conferir `pg_stat_activity` (sessão de humano/irmão = não derrubar)
- P-026 [processo][orquestrador][qa][passo-5][passo-6] Contagem de testes que sustenta gate é MEDIDA pelo orquestrador (comando canônico), nunca herdada do relato do agente; delta entre iterações fecha ARITMETICAMENTE (origem de cada teste + 0 deleções + 0 .skip/.only); divergência não reproduzível se registra como NÃO EXPLICADA — hipótese nunca vira causa; **reforço 20/08**: relato de agente não é evidência de ENTREGA — agente sinalizou conclusão sem ter editado nada; `git diff`/grep + contagem inalterada pegam em segundos, rotina pré-aceite de toda entrega — **reforço 21/08**: número AGREGADO certo não valida a DECOMPOSIÇÃO (total de 19 falhas certo desde o Passo 3, quebra por suíte com 3 erros); quem confere caso a caso usa a quebra, produzida por enumeração
- P-029 [processo][qa][ops][passo-6] Correção de defeito de gate sem guarda executável é meio conserto (o débito renasce um nível acima) — guarda de config de CI assere ESTRUTURA (job→passos→ordem→sem `if`/`continue-on-error`), nunca substring `toContain`, e se prova por mutação; **reforço 18/08**: allowlist (tabelas fora do RLS, segredos) se compara por IGUALDADE de conjunto — e é UMA por repositório; **reforço 19/08**: controle DETECTIVO que substitui preventivo (slugs no lugar de trava de custo) faz parte da decisão e nasce com teste que o pina (M6: apagá-los deixou 149/149 verde)
- P-030 [processo][qa][passo-6] Mutação reversível (defeito de volta → N testes caem → revert byte a byte) é a prova barata de que teste novo cobre algo — 0 quedas = teste decorativo; é a forma mais forte da reprodução independente (P-012) — **evoluída em P-034** (mutação como método sistemático de avaliação)
- P-031 [processo][team-leader][ops][passo-1][passo-6] Entregável-documento: o adjetivo ("executável por iniciante") vira checklist de aceite verificável na SPEC, e a avaliação lê como quem EXECUTA — 5 bloqueantes que a leitura elogiosa não acharia; forma 16/16 ≠ conteúdo executável
- P-034 [processo][qa][security][passo-6][gates] Teste de MUTAÇÃO é método de avaliação do Passo 6 em feature com gate/flag/custo (≥1 mutante por invariante do CONTRACT; placar injetados/detectados/sobreviventes no EVALUATION) — 4 revisões por leitura não viram que a feature podia nascer INERTE com suíte 100% verde (30/25/5 → 38/38/0); o avaliador REAPLICA os mutantes por conta própria com checksum da árvore, nunca aceita o relatório do dev; teto se assere por LITERAL (contra a constante = tautologia); evolui P-030; **reforço 19/08**: a mutação desceu para o IMPLEMENTADOR (3 agentes provaram a própria correção contando os vermelhos ao reverter) sem deixar de ser do avaliador (reaplicou 3) — pegou **tautologia** invisível à leitura (`filter(p).every(p)`, ver J-006); digest declara quantos testes ficam vermelhos ao reverter; **reforço 20/08**: mutação é o CRITÉRIO DE ACEITE de correção de bloqueante (a que reproduz o defeito original deixa a suíte vermelha) e as mutações de iterações anteriores se REAPLICAM na final; 26 mutantes/rodada — os 2 sobreviventes eram os 2 buracos reais; guarda composta exige fixture em que só a condição sob teste decide; **reforço 21/08**: mutante vivo SOBRE a linha que acabou de fechar bloqueante é o padrão (3/10 na it.2, os 3 sobre correções) — cada correção entra com ≥1 mutante NOVO do avaliador; placar separa reaplicados × novos · **reforço 26/08**: 10 correções revertidas — 8 vermelhas, 2 verdes (código certo, sem guarda): só a mutação separa "corrigido e guardado" de "corrigido e órfão"; órfã = débito declarado · **reforço 28/08** (`fidelidade-por-linha-monitorada`): 2ª feature INERTE com suíte verde no mesmo projeto — o único caminho de escrita não tinha spec e "apague a linha que grava" sobreviveu a 12130 testes com grep estrutural verde; o mutante mínimo de feature que escreve desce para a ENTREGA do Passo 5 (0 quedas = não está pronta); varredura `readFileSync`+regex prova forma, nunca comportamento
- P-035 [processo][team-leader][passo-1][git] Research que conclui estado de repositório sem `git rev-parse`/`status` do WORKING COPY diagnostica a branch errada com convicção (inspecionou branch homônima do assunto, 80 commits atrás, sem commit exclusivo) — RESEARCH §0 obrigatório: branch@sha + distância da main + símbolos-âncora conferidos NESSE HEAD por repositório; branch com nome parecido com o assunto é suspeita por default (`git log main..branch` vazio = ponto antigo, ignorar); afirmação sem âncora de HEAD não entra no Gate · **reforço 26/08**: vale para afirmação de AUSÊNCIA (lição/arquivo/coluna) — draft afirmou que R-007/R-008 não existiam e existiam desde a véspera; leva `grep -c`/`ls` e a saída
- P-036 [processo][security][ops][passo-6][gates] Marcador de exceção declarado NO artefato é permissão permanente (1 byte a mais passava) — exceção de gate é conteúdo aprovado por HASH em allowlist versionada (marcador E sha256); renovar é ato de rodada, nunca conserto de CI; provado por mutação (marcador removido/hash divergente ⇒ exit 1)
- P-037 [processo][ops][qa][passo-6] Runbook × código divergem em silêncio (6 slugs fantasmas + 8 faltando passaram por 4 leituras; metric filter sobre slug que ninguém emite nunca dispara) — paridade documento×código é TESTE nos dois sentidos (emitido ⊆ doc E doc ⊆ código, com campos), provado por mutação; generaliza N-026 · **reforço 26/08**: 5 slugs errados + 6 divergências achadas lendo o fonte no Passo 6; e o literal que o runbook CONSOME (`grep resultado=…`) se pina por teste — sem isso é débito declarado · **reforço 27/08**: slug `cofre-ausente` morreu na CONSOLIDAÇÃO e reapareceu no boot; fechou com spec de paridade nos 2 sentidos (`d4sign-gramatica-log.spec.ts`) — ver P-066
- P-038 [processo][qa][passo-6] Mutante mascarado por defesa em profundidade (`SKIP LOCKED` vs re-check do PG; pré-checagem vs índice único; saneador vs driver) — mutar cada guarda ISOLADA e juntas; teste que NOMEIA corrida sem reproduzi-la (segurar lock ≠ 2ª escrita com a mesma chave) passa com o defeito injetado; guarda estrutural sobre o TRECHO certo (a CTE, não o arquivo); entrada hostil é a que o BANCO rejeita
- P-039 [processo][qa][security][passo-4][passo-6] Campo de ENTRADA opcional aceito na borda, gravado, e nunca consumido a jusante (`replyTo`) não falha nenhum teste de unidade por camada — toda entrada opcional do DTO ganha 1 caso da borda ao fake do provedor; auditoria do §API nos dois sentidos (P-019 + este); recusa silenciosa também é caso
- P-040 [processo][qa][passo-6] `requestId`/uuid sorteado dentro de asserção "não ecoa a entrada" é flake garantido (`'42'` colide em ~25%) — varredura de eco exclui campos gerados pelo servidor; sonda de eco longa e improvável; teste que falha "às vezes" é achado com dono, fecha com 5 execuções verdes
- P-041 [processo][orquestrador][dev-backend][qa][passo-5][passo-6] Passo 5 em ondas paralelas por FRONTEIRA DE PACOTE (o que a onda 2 importa já compila) com decisões/avisos/assinaturas em `drafts/AVISOS.step5.md` (seção por papel, injetado na onda seguinte); teste do QA com bug de fixture é SINALIZADO e redirecionado com justificativa no arquivo, nunca apagado; o Passo 6 julga o AVISOS em seção própria
- P-042 [processo][orquestrador][passo-1][passo-6] Agente interrompido por mensagem do usuário ou derrubado por limite de uso é RETOMÁVEL por mensagem com contexto preservado (4/4 na rodada: 1 p1 + 3 avaliadores de ~2 h) — mensagem de retomada curta primeiro (custo ~zero); só sem tool-call em 5–10 min re-spawnar limpo (P-015); RUN.jsonl marca `retried` com a causa
- P-043 [processo][orquestrador][ops][passo-5] `infra_impact:false` ≠ "sem entrega de DevOps": o gatilho de invocação no Passo 5 é o ENTREGÁVEL do papel no CONTRACT (runbook/smoke/OPS-*), nunca a flag da SPEC — o runbook ficou sem dono até o gate reprovar (OPS-1110)
- P-044 [processo][orquestrador][qa][passo-6] Loop de correção INLINE pelo orquestrador (devs caídos) só com revalidação independente POR MUTAÇÃO por agente distinto — achou 2 furos (M6/M7) que releitura aprovaria; furo com produção certa fecha com guarda test-only do avaliador
- P-045 [processo][dev-backend][security][passo-3][passo-6] Guarda fail-closed de invariante financeiro decide POR CONSEQUÊNCIA, nunca por bloco: classificador de N ganchos devolve 3º estado (`INDETERMINADO`) e cada gancho decide — suprime onde agir é irreversível (documento fiscal), age onde NÃO agir pune quem pagou (restabelecimento); tabela gancho×postura no CONTRACT, provada por mutação
- P-046 [processo][dev-backend][dev-frontend][passo-6] Correção que MUDA o contrato de resposta (campo novo ou significado de campo) não está completa na camada em que nasceu — nomeia as DUAS pontas e a revalidação cobre o consumidor; alarme barato: grep do campo novo no repo consumidor, 0 ocorrência = correção pela metade (C-07→C-35: a tela passou a instruir cobrança dupla)
- P-047 [processo][qa][security][passo-3][passo-6][gates] Mutante derivado do ESTADO OPERACIONAL ALCANÇÁVEL (tabela destino/flag × ativo/reprovado/ausente, incl. estados que uma decisão de gate tornou possíveis) acha o que a lista acordada do CONTRACT não cobre — 15/15 mortos com 118 verdes e 2 dos 6 mutantes "fora da lista" do QA sobreviveram (v1 OFF + v2 ON); decisão de gate que muda o espaço de estados dispara revisão da matriz; aceite da correção = o MESMO mutante voltando a morrer, reaplicado pelo avaliador; lista acordada é piso, não teto — complementa P-034/P-038
- P-048 [processo][orquestrador][qa][security][ops][passo-6] Convergência de 2+ papéis no MESMO achado por caminhos independentes = bloqueante real: dedupe por CAUSA em 1 achado, severidade = máxima, preserve as N evidências, roteie 1 correção com ≥2 rotas sondadas — inverso do P-024; achado convergente vira lição de stack — **reforço 21/08**: B-065 (QA) + F-S1 (Security) convergiram no mesmo predicado fiscal; convergência define a severidade, não a solução (vias divergiram; escolha pelo runbook, P-051)
- P-049 [processo][orquestrador][qa][passo-6] Consolidação do EVALUATION e loop de correção rodam EM PARALELO quando o bloqueante é cirúrgico e já documentado nos drafts (arquivo:linha + fix sondado + teste que pina): coluna "Status it.N" pronta + seção "Revalidação it.N" vazia, revalidação independente preenche sem reescrever
- P-050 [processo][qa][ops][team-leader][passo-6][passo-7][gates] Aprovação de seção do EVALUATION por CLÁUSULA condicional pré-registrada no draft só vale com condição objetiva (arquivo:linha) + verificação por OUTRO papel com teste que morre ao reverter + ressalva visível + condição de go-live nominal; o estado de assinatura vive em 3 lugares (draft, consolidado, RUN.jsonl) — quem fecha a condição re-sincroniza o consolidado, e o Passo 7 diz qual vale se divergirem (nunca maquia)

- P-051 [processo][dev-backend][qa][security][ops][passo-5][passo-6][passo-7] Teste do Passo 4 que CONTRADIZ o CONTRACT: Dev implementa a letra, deixa o vermelho e registra em `AVISOS.step5.md` (é achado do contrato, não do teste) — entre vias equivalentes de correção, a que preserva queries/baselines do RUNBOOK vence (a do QA remediria o gate P3); SPEC ratificada no Passo 7 · **reforço positivo 27/08**: 6 vermelhos eram do TESTE (dublê `null ?? default`, vetor errado, allowlist envelhecida) — devs deixaram vermelho e devolveram; QA endureceu 3/6 (J-015)
- P-052 [processo][team-leader][dev-backend][qa][passo-1][passo-3][passo-4] "Fonte única" com nome de TABELA servindo 2 PERGUNTAS (agora × numa data × no período) é contradição disfarçada: predicado se nomeia pela pergunta; flag de ciclo de vida só no presente, histórico só por janela de datas; invariante que 2 predicados presumem vira spec de varredura escopada; paridade com fixture de 2 linhas e data ANTERIOR à troca; censo inclui SQL cru
- P-053 [processo][team-leader][qa][dev-backend][financeiro][passo-1][passo-3] Fórmula de dinheiro "por partes" com arredondamento por parte NÃO soma o total (3×10 dias sobre 89,90 = 89,91): invariante "Σ = total" é TESTE de SPEC com caso que arredonda mal; fórmula canônica = total corrido (último absorve o resíduo), partes adjacentes do mesmo item se fundem, util puro único para prévia e emissão
- P-054 [processo][ops][security][dev-backend][financeiro][passo-1][passo-3][passo-6] Feature que dá EFEITO FINANCEIRO a uma tabela: censo dos ESCRITORES existentes (CRUD cru sob permissão ampla vira fabricador de item de fatura ⇒ apagar, não "sem link"), censo do DADO REAL contra o invariante novo ANTES do deploy (linha duplicada inerte vira cobrança +3,33 %), rota manual do motor é 2º produtor no censo RBAC, dry-run em produção é o detector antes×depois, clamp no TOTAL
- P-055 [processo][qa][dev-backend][dev-frontend][passo-4][passo-5][passo-6] Teste adiado no Passo 4 "porque o alvo não existe" precisa de DONO no Passo 5 (3 voltaram como reprovação na it.1; 1 era furo real só visível por HTTP); decisão revertida de rodada anterior exige varrer testes por REFERÊNCIA ao artefato removido (CONTRACT previu 2 guardas, eram 3)
- P-056 [processo][qa][passo-4][passo-6] Âncora de regressão por padrão de NOME de arquivo tem ponto cego onde a feature toca resolver COMPARTILHADO (100 % verde com regressão real no repo) — âncora = nome ∪ SÍMBOLOS tocados pelo diff (`grep -rl` dos identificadores alterados; revelou 11 suítes invisíveis, 1 vermelha); `{pattern}` do STACK.md é ponto de partida, nunca a âncora; suíte completa com delta zero segue obrigatória · **reforço 27/08**: símbolos IMPORTADOS entram na âncora, mas com DENYLIST de framework (`Prisma`/`Logger`/`Injectable`…) — sem ela 102 → 296 suítes e a âncora deixa de discriminar
- P-057 [processo][qa][ops][dev-backend][passo-6] Teste vermelho por AMBIGUIDADE de identificador: perguntar primeiro se a ambiguidade está no ALVO (log/mensagem repetindo o mesmo nome em 2 linhas), não no seletor — apertar o matcher esconde o defeito que o runbook/plantonista também não distingue; corrigir a MENSAGEM; contagem operacional por PREFIXO de linha, nunca por slug compartilhado (o `grep -c` do runbook dobrava a contagem)
- P-058 [processo][qa][dev-backend][passo-5][passo-6] Prova apagada ("spec temporário, rodei, apaguei") só vale com o ramo DECLARADO nominalmente como não coberto — sem a lista, "verifiquei" é indistinguível de não ter verificado; destinos legítimos: spec fixado na mesma onda ou item nominal (arquivo:linha) na lista de não-coberto; avaliador confere pelo nome
- P-059 [processo][ops][security][orquestrador][gates][passo-3][passo-5][passo-6] Auditar FIDELIDADE ao contrato não pega erro de projeto do AUTOR: item implementado pelo próprio avaliador exige o critério extra "este é o lugar certo?" (arquivo, momento boot×request, fonte env×banco, raio de dano); Gate 3 exige "onde liga, quando roda, o que lê" p/ todo item de observabilidade; `main.ts`/bootstrap é zona compartilhada — só justificativa de subida; checagem removida do boot ganha substituto nomeado no runbook (P-203)
- P-060 [processo][dev-backend][security][qa][financeiro][passo-3][passo-4][passo-6][passo-7] Reserva de recurso PAGO só reverte com certeza TIPADA de não-envio (desfecho `NAO_ENVIADO|INDETERMINADO` vem do PROVIDER, nunca regex sobre erro; ausente/indeterminado ⇒ NÃO reverte — aviso perdido é detectável, efeito entregue não volta); reserva/revert CONDICIONAIS no `WHERE`; teto conta TENTATIVA (`EM_ENVIO∪ENVIADA∪ERRO`) e por ORIGEM; SPEC "reverte em erro" corrigida e ratificada no Passo 7
- P-061 [processo][qa][security][passo-4][passo-6] Invariante "nunca 2×" de recurso pago se mede sobre ENTREGA na trilha, não sobre chamadas ao dublê — teste que conta chamadas proíbe a retentativa legítima (reserva revertida por não-envio comprovado); bateria ganha o caso POSITIVO da retentativa e o negativo do indeterminado
- P-062 [processo][ops][devops][qa][passo-5][passo-6][gates] Gate de runbook (grep/SQL de prévia/controle) só vale EXECUTADO contra o artefato real + controle NEGATIVO (4 vereditos falsos numa rodada: grep casando o próprio runbook, prévia com predicado ≠ código listando todo mundo já avisado, controle detectivo cego por `LIKE` de prefixo) — query de prévia REPETE o predicado do código com `arquivo:linha`; chave do controle amarra TODAS as dimensões da idempotência; **reforço 25/08**: grep de segredo do runbook acusou a própria pasta de avaliação (5 linhas de prosa, 0 credencial) — controle negativo inclui `--exclude-dir=specs/features`; greps de migration sobre `sed 's/--.*$//'` com par `exec × cru` · **reforço 27/08**: pré-check de runbook com `data_exclusao` em tabela sem a coluna (3ª vez) — pego só porque foi EXECUTADO contra o DDL · **reforço 27/08**: 3 gates de runbook errados antes de executar (grep de teto com 4 falsos positivos, "gate OFF ⇒ 0 linha" falso — heartbeat sai sempre —, slot `:45` × `*/15`) — corrigidos porque rodados contra o código
- P-063 [processo][team-leader][security][produto][passo-1][passo-3][gates] Mensagem a cliente final tem eixo REGULATÓRIO: separar "obrigação de avisar" (RGC Art. 50) de "publicidade/venda" (Art. 4º XIX — consentimento prévio que a base não tinha); seed informativo + frase neutra de canal; aprovação marketing+jurídico é bloqueante de go-live com `sha8` do texto; denylist é controle FRACO declarado
- P-064 [processo][orquestrador][qa][devops][dev-backend][passo-6] Correção do Passo 6 em ONDAS (QA-guardas ∥ DevOps-docs ∥ Front → Backend por último): `cp`-restore de um mutador sobrescreve a edição de outro; **um único mutador** na revalidação, árvore quieta (`mtime` < build); a onda de docs cita `arquivo:linha` e é re-verificada contra o HEAD DEPOIS da última onda de código (o `§37.6` inverteu de sentido 2×); ondas registradas como `note` no RUN.jsonl
- P-065 [processo][security][ops][devops][passo-3][passo-6][gates] `.env` de DESENVOLVIMENTO com URL+credencial REAL de terceiro cobrado sobe `configurado=true` contra a base real (nenhum teste vê); parceiro com UMA URL ⇒ `sha8(hml)==sha8(prod)` é o estado inicial — inventariar `.env` locais no Passo 3, boot anuncia `sha8` em dev, decisão ESCRITA (esvaziar × aceite nominal com condições verificáveis), e o aceite de dev NÃO levanta o bloqueio hml×prod (aceite específico com alvos nominais + teto `'0'`, ou par de homologação do fornecedor)
- P-066 [processo][dev-backend][devops][ops][passo-3][passo-4] Consolidação que DESCARTA item de draft sem rastro cria requisito fantasma (slug `cofre-ausente` sumiu do CONTRACT sem "conflito resolvido"; Dev implementou 8 slugs, runbook/`.env.example` diziam 9; visto só no boot) — CONTRACT ganha "Itens de draft não absorvidos" (id, motivo, substituto); cada autor confere o consolidado contra o próprio draft antes do gate; listas operacionais (slugs/causa/env/jobs) em UM lugar com teste de paridade já no Passo 4; budget nunca justifica apagar item operacional
- P-067 [processo][orquestrador][qa][dev-backend][passo-6][gates] Mini-iteração de HIGIENE após APROVADO: 🟡 que enfraquece a prova/proteção de um 🔴 fechado (guarda flaky do bloqueante, código morto que contorna a allowlist nova, fallback que trunca) não é débito — 1 dev com escopo fechado, QA verifica por `arquivo:linha`, ESCREVE o caso que faltava e anexa "it.2b" sem re-veredito das seções; ~1 h contra a reabertura do bloqueante na rodada seguinte
- P-068 [processo][team-leader][dev-backend][qa][passo-1][passo-3][passo-6][passo-7] Recorrência mensal com clamp de fim de mês tem DUAS semânticas — iterativa sobre o clampado (o 31 morre para sempre ao cruzar fevereiro) × `base + k·P` (transitória) — o CONTRACT escolhe com vetor discriminante (`31/08, P=6, 3 rolagens` + bissexto); nunca `f(f(base,P),P)`; teto de iterações mantido; quando a decisão vira, o teste que FIXAVA a semântica antiga é parte do diff (reescrever sentido, conferir vetores pela aritmética antes de rodar); SPEC ratificada no Passo 7
- P-069 [processo][team-leader][dev-backend][qa][passo-1][passo-3][passo-5][passo-6][git] Rodada de REAPONTE sobre working copy de rodada revogada (nunca commitada): reapontar saiu muito mais barato que refazer, mas o inventário APROVEITA/REAPONTA/DESCARTA tem de listar CADA arquivo — código, SPECS, migration por seção, docs, allowlists — 5 specs REAPONTA apagadas sem substituto (~111 testes) passaram por "remoção legítima" porque spec que nunca esteve no HEAD é invisível ao delta por lista de FAIL; antiarmadilha assere 0 órfão; cobertura se audita por cenário contra o §3 + mutação; higiene (redação de log) é obrigação transversal de arquivo novo
- P-070 [processo][qa][devops][ops][passo-5][passo-6] Guarda de AUSÊNCIA ("0 ocorrência do literal X") × runbook que precisa citar X para ensinar a conferir: dois artefatos da mesma rodada se contradisseram (3 testes vermelhos) — o runbook monta o literal por CONCATENAÇÃO (`'A' || '_B'`), a guarda mantém o runbook no escopo com controle positivo, e a correção é sempre do lado do artefato, nunca afrouxando a guarda; paridade de ausência, irmã de P-037
- P-071 [processo][team-leader][qa][passo-1][passo-6][gates] Emenda de regra de negócio depois do Gate 1 ("pré-pago não tem fidelidade") se responde com "o que o código faz HOJE" (`arquivo:linha`, fonte e `dist`): `calcularMulta` não conhecia modalidade ⇒ achado de produção + condição de go-live SEQUENCIADA fora da rodada (correção direta antes do gate `'S'`, com sonda em prod), a rodada FIXA a divergência como teste (com controle negativo e instruções de inversão) e a API carrega o `motivo` — nunca implementar em cima do tripwire de dinheiro
- P-072 [processo][git][jest][devops][team-leader][qa][passo-6][passo-7] Commit SELETIVO em árvore compartilhada quebra allowlists COMPARTILHADAS (11 specs citam as 2 migrations; o commit de uma rodada deixa a outra sem pasta) — e o sinal vem MASCARADO quando os specs já estão vermelhos por dívida: hunk compartilhado se identifica antes do commit (conjunto ou lista editada à mão), conferência por NOME de teste, vermelho crônico se quita ou ganha `skip` justificado
- P-073 [processo][team-leader][devops][ops][passo-1][passo-3][passo-5] Coluna nova que carrega DECISÃO do operador sem superfície de escrita é incidente adiado (`ADD COLUMN` sem seed e sem tela ⇒ valor entra por SQL em prod); config morta no `.env` desvia o diagnóstico — RESEARCH responde "quem preenche, por onde, e o que acontece vazio"; sonda de vazio no runbook; env que morre é apagada na mesma rodada; o erro do operador aponta a tela

## Segurança (PROCESS.md, tags [security])

- P-101 [security][passo-3][passo-6] Endurecer identificador público sem auditar a RESPOSTA = fechadura nova em porta aberta — a superfície importa mais que o id; compat é com os campos consumidos, não com o payload inteiro (whitelist DTO)
- P-102 [security][passo-1][passo-7] Segurança × retrocompat: dual-resolve por formatos disjuntos + ramo legado como débito com gatilho OBJETIVO; o sinal de observabilidade nasce na mesma entrega (inclusive no caminho de erro); enforcement que pode bloquear receita (429 sob CGNAT) entra em modo observação primeiro
- P-103 [security][ops][qa][passo-3][passo-5][passo-6] O controle de DETECÇÃO é superfície de ataque: corpo/erro de terceiro entrando no slug de log sem achatar quebra de linha forja a linha `[marca] … resultado=OK` que o runbook grepa (e substring solta infla o contador) — choke point de higiene (classe `[\x00-\x1F\x7F\u0085\u2028\u2029]` + marca ecoada → `[eco]`) em TODO campo variável, ordem redige→higieniza→trunca, grep do runbook ANCORADO na marca colada ao campo; código E comando são as 2 metades; mutação em 3 direções; threat model pergunta "o que o destino pode escrever no meu log?"
- P-104 [security][team-leader][devops][passo-1][passo-3][passo-6] Credencial de parceiro se CLASSIFICA antes do go-live (formato, claims, `exp`, escopo, TITULAR — decodificar só os nomes de claim, localmente): "bearer estático por empresa" era JWT de sessão de PESSOA FÍSICA sem `exp` ⇒ vazamento permanente até revogação pelo fornecedor, integração morre por evento de RH, escrita auditada como aquela pessoa, PII de terceiro no segredo — pedir credencial de serviço como condição de go-live; playbook de comprometimento ≠ rotação; `sha8` não prova troca de token
- P-105 [security][devops][ops][passo-3][passo-5][passo-6] Runbook de revogação por sessão é NO-OP para conta de máquina que pula o cross-check de sessão: revogação tem 2 metades e a 2ª depende do TIPO de conta (integrador = desativar a credencial de bypass, que corta na request seguinte) — tabela por tipo de conta com lever + prova; o risco real é o plantão ver o marcador preenchido e concluir que revogou

## Ops (PROCESS.md, tags [ops])

<!-- - P-2xx [ops][passo-5] Título curto — lições do agente DevOps ficam em PROCESS.md com tag [ops]; as específicas de stack vão para stacks/<perfil>.md -->

- P-201 [ops][dev-backend][passo-3][passo-5] Endpoint com produtor único e SEM reconciliação: só logar sucesso torna "produtor parado" e "produtor em erro em loop" indistinguíveis — helper `recusa()` (warn com slug estável + ids, sem PII) em TODO caminho 4xx; query de detecção manual exige baseline + dono + frequência
- P-202 [ops][passo-5][passo-6] Build que muta arquivo versionado é bug de gate: CI roda build completo + `git diff --exit-code` SEM path (diff com path é cego fora dele); arquivo que o framework escreve por design é pré-declarado + ignorado no lint; ordem de steps não é defesa
- P-203 [ops][gates][passo-3][passo-5][passo-6] Decisão de gate que REMOVE a rede de segurança (dry-run recusado em feature com efeito irreversível e cobrado) **transfere** a garantia para o runbook: o mesmo gate registra os substitutos com dono e verificação — trava automática com **limite superior no código** (parâmetro só reduz), **ensaio com o gate desligado** (prova a cadeia com 0 rede), janela curta escopada a 1 registro com prova "efeito → trilha → log → contagem exata" e teto de disparos, autorização nominal registrada, e **censo de quem pode mexer nas travas** (trava que qualquer credencial afrouxa não é trava); "não implementamos o dry-run" é mudança de RISCO, não linha de escopo
- P-204 [ops][devops][git][passo-5][passo-6] Guarda de paridade `git diff <base>..HEAD` compara COMMITS (verde com a mudança no working copy) e não vê untracked: usar `git diff <base>` + `git ls-files --others` + controle positivo; em branch-trem a guarda é ESCOPADA ("só arquivo X com chaves Y"), nunca "vazia"; corrigir TODOS os runbooks que copiaram o molde
- P-205 [ops][devops][security][passo-3][passo-5][passo-6] Kill-switch de feature declara sua COBERTURA: gate do job (`X_JOB_ATIVA='N'`) calou 3 dos 5 eventos — os 2 disparados por ROTA são síncronos e sem gate, e o nível 2 do rollback não os cala (só desvincular o grupo/canal) — CONTRACT traz a matriz gatilho × gate, runbook lista o que CONTINUA acontecendo após cada nível, rollout começa com destino de teste; irmã de P-203

## Stack: Jest — transversal (stacks/jest.md)

- J-001 [jest][qa][dev-backend] `jest.clearAllMocks()` NÃO limpa a fila de `mock*Once` — `mockReset()` no `beforeEach`; resíduo de fila causa falha dependente de ordem
- J-002 [jest][qa][security][passo-4] Caminhos-irmãos de um guard (ausente × divergente) assertam o MESMO invariante (`not.toHaveBeenCalled()` do efeito externo) — só o status de erro mascara fail-closed violado em ação irreversível com suíte verde; **reforço 04/08**: bateria de idempotência exige fake COM ESTADO (honra `where`, simula rollback) + log de escrita não-revertido (pega escrita tentada-e-desfeita) + helper `nadaEscrito()` compartilhado por todos os ramos de recusa — mock de retorno passa por construção
- J-003 [jest][qa][passo-4] Fixture de dado que o PRÓPRIO sistema produz deriva o shape do código PRODUTOR (FKs inclusive; 1 variante por origem) — fixture "conveniente" com FK nula que produção nunca gera = suíte verde contra dado inexistente; **reforço 31/07**: feature com adapter externo exige ≥1 teste linha-do-banco→adapter REAL (incl. caminho fail-closed) — sem ele, 322 testes verdes conviveram com feature 100% inerte; **reforço 01/08**: o "produtor" inclui a PROJEÇÃO — select estreitado exige fixture do mapper reescrita A PARTIR do select (fixture larga fica SÓ nos testes de vazamento); total soma direto das rows, agregação sem id SOMA, nunca sobrescreve
- J-004 [jest][qa][dev-backend] `npx jest` avulso NÃO carrega a config do workspace ⇒ erro de parse de TypeScript FALSO em linha válida (sem transform/moduleNameMapper) — rodar sempre pelo script do projeto; erro de sintaxe que só aparece numa forma de invocação é do transform, não do arquivo
- J-005 [jest][qa][dev-frontend][passo-4][passo-6] Verificação estrutural por regex do fonte (`toMatch(/https:/)`) tem teto baixo — passa com a string em comentário; lógica em função pura EXPORTADA ganha teste de unidade real com entradas hostis mesmo sem testing-library; regex fica restrita a atributos JSX/imports (é lint, não teste de comportamento); **reforço 25/08**: guarda de "fonte única" por CONTAGEM de string (`/Rótulo</g` = 1) premiou a duplicação (Fragment vazio só para produzir o `<`) — asserir uso do SÍMBOLO (≥2 usos), não aritmética de literais
- J-006 [jest][qa][passo-4][passo-6] Asserção derivada da PRÓPRIA filtragem é tautologia (`filter(p).every(p)` = verde para qualquer entrada) e asserção por PRESENÇA (`toMatchObject`) não pega vazamento **aditivo** (2 chaves com PII acrescentadas ao payload passaram uma rodada inteira) — invariante de coleção se assere por **igualdade de conjunto** (`Object.keys().sort()` × constante escrita à mão) e só conta como cobertura **depois de mutada**
- J-007 [jest][qa][passo-4][passo-6] Invariante caro com 2+ consumidores se assere POR CONSUMIDOR (recorte por símbolo, nunca `toMatch` no arquivo inteiro; função pura coberta ≠ call site coberto) — 2 mutações sobreviveram com 1168/1173 verdes na mesma rodada; prova = mutação por consumidor; **reforço 20/08**: caso extremo — função pura com 24 testes e ZERO call sites (requisito nº 1 nunca chegou à tela com a bateria mais numerosa verde); util "fonte única de N superfícies" ⇒ o caso ENUMERA as N (grep do call site é asserção válida) · **reforço 26/08**: variante backend — guarda de custo em 2 dos 3 pontos de entrada (faltava no ramo de pendentes do job) ⇒ 1 caso por ponto de entrada com caso-controle; mutar cada guarda isolada
- J-008 [jest][qa][passo-4][passo-6] Teste contra o eco do próprio dublê: regra que mora no repositório testada só contra o mock é teste da fixture (B-030), e comparar a resposta com o argumento passado ao mock FIXA o bug (tautologia diferida, prima da J-006) — esperado literal + distinguir o que a resposta anuncia do que devolve; **reforço 21/08**: spy SEM injeção é tautologia — "0 chamada à operadora" só vale com o fake no colaborador que faz a chamada (o serviço REUSADO, não o da feature) + caso-controle em que a chamada acontece e o spy a vê · **reforço 26/08**: dublê FIEL ao produtor é pré-condição de teste de ponto fixo (sincronizar 2× ⇒ mesmo estado TERMINAL); oráculo cruzado que extrai por regex exige controle negativo (extração vazia ⇒ vermelho)
- J-009 [jest][qa][passo-6] Mutante que passa a matar MENOS testes entre iterações (23 → 2) pode ser defesa que DOBROU, não teste podre: diagnosticar nominalmente; o que RESTA matando é a lista do que só aquela linha garante ("não remover: única fonte de X"); camada mascarada ganha mutante próprio
- J-010 [jest][qa][ops][passo-6] Teste de janela de tempo (429/`Retry-After`/cooldown) é flake sob paralelismo da suíte completa: fake timers; regressão em árvore compartilhada conta por ARQUIVO + CAUSA com 2ª execução antes de virar achado; arquivo intocado que falha só sob carga é débito do dono, não regressão; **reforço 25/08**: suíte flaky que cai INTEIRA reduz o total e é compensada por outra — só o nome revela (J-012)

- J-011 [jest][typescript][qa][dev-backend][passo-4][passo-6] Spec `.ts` sem `import`/`export` é SCRIPT no escopo global: `function util()` em 2 arquivos = TS2393 nos dois, suítes não compilam (0 testes, somem da contagem) e a função testada fica com cobertura ZERO em silêncio — `export {};` na 1ª linha / `isolatedModules`; suíte que não compila na baseline é achado, não "pré-existente" (carona bloqueante se a feature depende da função; +104 testes recuperados)
- J-012 [jest][qa][security][devops][passo-6] Avaliadores em paralelo na MESMA árvore corrompem a medição (mutação concorrente = 5 baterias vermelhas disjuntas e 314/340; 2 `jest` no mesmo comando = 10387 × 10442) — 1 `jest` por vez, 1 mutador por vez, árvore quieta (`mtime` < build, senão "não medi"); dívida de suíte se confere por NOME + causa (3 flaky fora do recorte oscilaram em 5 execuções com o total "19" batendo), nunca pelo total · **reforço 27/08**: 2 `jest` concorrentes derrubam suítes alheias por timeout 5 s — falso flake; `--runInBand` confirma delta mas o OFICIAL é a paralela sem carga; serializar baterias entre avaliadores (N-051)
- J-013 [jest][qa][security][passo-6] Mutante morto pelo COMPILADOR não é guarda (16 "vermelhos" = suíte não compilou; 0 asserção sobre o invariante de PII) — mutante de aceite type-correct, relatório nomeia QUAL asserção caiu por mutante, "conjunto fechado de chaves" se assere por igualdade de conjunto no ponto real de escrita; sobrevivente-no-compilador vira BURACO no consolidado
- J-014 [jest][qa][dev-frontend][dev-backend][passo-4][passo-6] Asserção que pina um MOMENTO (`slice(-2)`, "X é o último", posição absoluta) em vez da REGRA (ordem relativa, contiguidade, conjunto) faz toda união aditiva futura virar falsa regressão — enunciar a regra no `expect` e no título; mudança em teste alheio é DESVIO registrado (UX/UI no mérito + QA no processo, ambos valem), critério "byte-a-byte" vira "verde + mudanças declaradas"
- J-015 [jest][qa][dev-backend][dev-frontend][passo-4][passo-5][passo-6] Dublê com `overrides.x ?? default`/spread reintroduz o default ⇒ caso negativo INALCANÇÁVEL por qualquer implementação (`=== undefined`/`'x' in`, nunca `??`; caso-controle "`null` chega ao alvo"); vetor numérico sem a conta atravessou 2 passos e virou alarme de laço infinito; allowlist fixada antes dos arquivos existirem varria 7/13 (caso anti-envelhecimento contra o disco); guarda estrutural separa ESCREVER de LER por sintaxe, nunca por nome de arquivo; dev deixa o vermelho e devolve, QA endurece
- J-016 [jest][qa][dev-backend][passo-4][passo-5][passo-6] Spec que DELEGA cobertura no cabeçalho ("exercido nas specs de serviço") é cobertura fantasma quando o destino não existe; spec apagada em reaponte nunca esteve no HEAD ⇒ delta por lista não a vê; grep estrutural de gate/teto verde ≠ comportamento — o mutante mínimo de feature que ESCREVE é "apague a linha que grava" (M8 sobreviveu a 12130 testes; na it.2 mata 12): helper de escrita com spec própria (round-trip no `data` do `update`) + 1 caso por gatilho na mesma `tx` + contraprova executável de toda delegação
- J-017 [jest][qa][security][passo-4][passo-6] Varredura por diretório/glob sem PISO passa vazia quando o caminho muda (3 controles de segurança verdes examinando 0 arquivos); detector por regex de UMA forma sintática (template literal) é cego a aspas simples (runbook 19 × código 20 slugs) — piso escrito à mão, controle positivo por forma, mensagem nomeia a contagem, mutante "mova o alvo", conserto acoplado à paridade

## Stack: NestJS (stacks/nestjs.md)

- N-001 [jest][qa] Jest 30: a flag é `--testPathPatterns` (plural)
- N-002 [nestjs][dev-backend] `Logger` não declarado em repositório trava o watch mode (TS2339)
- N-003 [nestjs][dev-backend] `console.log`/`util.inspect` trunca aninhamento (depth 2) — Logger + JSON.stringify
- N-004 [prisma][dev-backend] Cast TS (`as unknown as`) não converte em runtime — converter no repositório
- N-005 [prisma][qa] Cliente Prisma gerado stale derruba specs não relacionados — rodar db:generate antes
- N-006 [prisma][jest] Jest com mocks não pega erro de tipo Prisma — rodar `build`, não só `test`
- N-007 [prisma][dev-backend] N+1 insert em `$transaction` estoura 5s (P2028) — usar createMany em lote
- N-008 [prisma][dev-backend] Efeito externo é pós-commit; auditoria acoplada ao estado é intra-transação
- N-009 [nestjs][dev-backend] ValidationPipe global sem `transform` não converte DTO de query
- N-010 [jest][dev-backend] Método transacional gigante não é testável — extrair lógica pura p/ *.util.ts
- N-011 [prisma][dev-backend][passo-5] Escrita externa com custo real: recomputar invariante + pré-persistir id de idempotência NA tx (advisory lock); provider pós-commit; nunca confirmado fantasma
- N-012 [prisma][dev-backend][ops] Migration com `INSERT...SELECT`/`ON CONFLICT` dependente de linha/unique pré-existente vira no-op silencioso — queries de pré-requisito + conferência pós-aplicação · **reforço 27/08**: variante RUIDOSA — seed citando COLUNA nascida em migration PENDENTE ⇒ `42703` + rollback total sob `--single-transaction`, achado só pelo DevOps no dump; cabeçalho declara a migration de origem de cada coluna + sonda de schema (`information_schema.columns`) no runbook; spec cruza colunas citadas × DDL dos dumps
- N-013 [nestjs][seguranca][dev-backend] Hash de segredo de ALTA entropia (≥256 bits, `randomBytes`) ≠ hash de senha: SHA-256 puro sem salt/KDF (KDF lenta = ~100ms/login sem ganho; salt inviabiliza comparação por igualdade); prefixo de algoritmo (`sha256:`) desambigua hash × texto plano ⇒ migration idempotente + login fail-closed
- N-014 [nestjs][seguranca][dev-backend] Parâmetro OPCIONAL numa guarda de authz (`x !== undefined && regra`) fura a regra em silêncio no 2º call-site — argumento obrigatório na assinatura; teste com o argumento omitido
- N-015 [prisma][dev-backend][qa][passo-6] Lookup compartilhado com labels homônimas: leitura escopada × escrita solta = guarda fail-open (efeito com custo repete) — `escrita ⊆ leitura` por construção (união escopado∪solto fail-closed) + fixture com homônimo hostil e fake que honra o `where`
- N-016 [nestjs][seguranca][qa] Leitura de domínio que lança 404 com o id na mensagem reusada em rota pública = oráculo de existência (e o `throw` genérico abaixo é código morto) — wrapper captura→relança genérico + teste anti-oráculo comparativo por rota (Set size 1)
- N-017 [nestjs][prisma][seguranca] Update parcial sem DTO: ramo de validação decide pelo estado EFETIVO (`payload ?? banco`), nunca pelo tipo do payload — Prisma ignora `undefined` e o PUT parcial burla a regra; teste do exploit + não-regressão na bateria · **reforço 27/08**: mesmo com DTO — coerência entre campos (fim < início GRAVADO) se decide sobre `payload ?? banco` dentro da tx, só quando o PUT toca o campo
- N-018 [prisma][dev-backend][ops] Migration de rename de valores exibidos: alvo por JOIN + IGUALDADE estrita — nunca LIKE (`_` é curinga: 'PLANO_%' casa PLANO_LOGISTICA_*); âncora redundante contra vizinho homônimo; conferir no schema se a tabela tem colunas de auditoria antes de carimbar (42703); rollback inerte comentado
- N-020 [prisma][dev-backend][qa] FK sem RELAÇÃO declarada no schema devolve só o id — o objeto esperado chega `undefined` em silêncio e a feature degrada como se estivesse funcionando; declarar relação + `include`, tipar o mapeador pelo retorno REAL (nunca `as`), testar da linha crua ao consumidor
- N-019 [prisma][jest][qa][dev-backend][passo-5] Spec de migration executável: readFileSync do .sql + tokenizar os UPDATEs — whitelist de SET, larguras por CARACTERE, valor antigo em todo WHERE, anti-LIKE, rollback 100% comentado; substitui prosa/grep manual; o gate empírico segue sendo staging (N-012)
- N-021 [prisma][dev-backend][ops][passo-5] Guarda de constraint por `conname` sem `conrelid` falha ABERTA: conname é único POR RELAÇÃO — homônimo em outra tabela pula a FK em silêncio com migration "bem-sucedida"; toda guarda E conferência filtram `conname AND conrelid='<tabela>'::regclass` (+ `confrelid` na conferência)
- N-022 [prisma][ops][passo-5] Sonda de `--single-transaction` por grep `^(begin|commit)` acusa falso positivo em `DO $$ BEGIN` (bloco PL/pgSQL, não transação) e induz a aplicar SEM atomicidade — remover blocos $$…$$ antes do grep ou casar só `BEGIN\s*(;|TRANSACTION|WORK)`; na dúvida, default é COM `--single-transaction`
- N-023 [postgres][qa][dev-backend][passo-4][passo-6] Idempotência checada (`EXISTS`) ANTES do lock é corrida aberta — checar depois do `FOR UPDATE` que o efeito já toma; o teste dispara T2 com T1 aberta segurando o lock (senão é sequencial disfarçado), assere estado final + contagem de linhas, e se prova rodando contra a versão ANTIGA · **reforço 27/08 (Prisma `updateMany`)**: marcador condicionado só por PK+marcador regrava o período antigo sobre o novo (contrato nunca mais avisado) — `where` repete a ponta de dado LIDA + elegibilidade, `count=0` = no-op; lock que relê mas não REVALIDA gera 2 eventos/2 e-mails — a releitura decide (`duplicada` ⇒ 0 update/0 evento/0 disparo)
- N-024 [postgres][security][dev-backend][passo-6] `set_config(...,true)` é local à TRANSAÇÃO: SECURITY DEFINER que arma GUC de tenant recusa contexto divergente (42501), guarda/restaura o valor anterior e ganha teste dos 3 casos (divergente/legítimo/bootstrap) — policies não protegem do que roda por dentro delas
- N-025 [postgres][qa][ops][passo-4][passo-6] Teste de isolamento cross-tenant sem linha semeada é vacuamente verdadeiro (73/81 tabelas com `count=0` sobre vazio) — sweep de catálogo garante a estrutura; a prova comportamental exige linha real do tenant A antes do count sob B; tabelas não-semeadas são débito visível, não cobertura; **reforço 12/08**: vale para passo de RUNBOOK — verificação que passa com o sistema desligado é pior que ausente (semear 2 contas, exigir 1 da A e 0 da B)
- N-026 [nodejs][config][qa][ops] Variável obrigatória sem default fora do `.env.example` só quebra em clone limpo — teste de paridade schema×template (toda chave obrigatória sem default presente no template) fecha a classe; segredo de exemplo é descartável por escrito
- N-027 [nodejs][config][security][dev-backend][ops] Segredo opcional com `''` passa no schema e vira fail-open (HMAC com chave vazia) — colapsar `''→undefined` na fonte única + provar a recusa nas duas pontas (schema E boot real com 503/evento próprio); **reforço 19/08 (NestJS/class-validator)**: env opcional é `@IsOptional() @IsString()` — `@IsNotEmpty()` derruba **o boot inteiro** onde a var ainda não foi provisionada; propriedade **não declarada** na classe passa sem validação (asserir que existe declarada + que o boot sem ela não lança)
- N-028 [nats][dev-backend][ops][passo-5] JetStream 2.11: `backoff` na config do consumidor SOBRESCREVE `ack_wait` (30s→1s medido) e `update` não remove campo — backoff vai no `nak(delay)`; config de consumidor segue expand/contract como migration
- N-029 [postgres][security][dev-backend][qa][passo-6] `jsonb` recusa NUL em valor E em CHAVE + surrogate solto (22P05/22P02) — `replaceAll` do escape fabrica veneno novo; sanear no serializador contando barras, truncar ANTES de varrer, medir contra o Postgres real; SQLSTATE pode vir no `cause` do ORM
- N-030 [prisma][postgres][dev-backend][security][ops][passo-5][passo-6] `pg_advisory_lock` de SESSÃO × pool do Prisma: adquire numa conexão e "libera" em outra ⇒ retido até a conexão morrer; `xact` exigiria transação aberta durante chamadas HTTP longas (viola N-008) — para lote com rede: freio de processo (`emExecucao` antes de qualquer leitura + 409 + `finally`) + cooldown em memória só para resultado determinístico (404, não 429/timeout), limitação "1 processo" DECLARADA no código e VERIFICADA no script de deploy (PM2 fork sem `-i`; restart — incl. `--max-memory-restart` — zera o cooldown); multi-instância/rotina ⇒ freio no banco; provado por mutação (sem freio 2×, sem cooldown 3 chamadas = 1)
- N-031 [drizzle][postgres][dev-backend][qa][passo-5][passo-6] `db.execute(sql\`…\`)` do drizzle devolve `timestamptz` como TEXTO (o builder tipado devolve `Date`) — `texto <= Date` em JS é `false` em silêncio (janela de 24 h inexistente); conversor único na borda (`instante()`), carimbo volta ao banco como veio, teste da janela contra o banco real com linha FORA da janela
- N-032 [nodejs][tsx][dev-backend][ops] `tsx` resolve o `tsconfig` pelo CWD, não pelo arquivo — app/script com `paths` só sobe pelo script do PRÓPRIO pacote (`pnpm --filter`); utilitário compartilhado importa por caminho, nunca por alias; erro de resolução que só aparece numa forma de invocação é do cwd (irmão de J-004)
- N-033 [pnpm][ops][dev-backend][passo-5][passo-6] `pnpm install --frozen-lockfile` reprova importer VAZIO — dep adicionada com `--offline`/à mão deixa `apps/x: {}` no lockfile e só o CI vê; rodar o comando do CI localmente após toda mudança de dep (gate do dev e do avaliador); `--offline` é para rodar, não para declarar
- N-034 [nats][dev-backend][qa][passo-3][passo-5] `Nats-Msg-Id` de evento DERIVADO (status `sent/delivered/read` do mesmo wamid) é composto `<id-base>:<discriminador>` (discriminador pelo charset fechado) — id puro faz o JetStream engolir o 2º/3º como duplicata sem erro (timeline para no 1º); subject continua o da entidade (id deduplica, subject roteia); teste com broker real assere 2 entregas
- N-035 [nestjs][prisma][dev-backend][security][passo-5][passo-6] Adapter que promete "nunca lançar": o `try` abre no **primeiro degrau com I/O** (a consulta ao repositório fora dele faz erro do Prisma escapar e o motor persistir `err.message` **cru** em coluna servida por API); log de **sucesso** dentro do `try` transforma efeito **entregue** (irreversível) em `ERRO` na trilha ⇒ todo log por helper que engole falha do transporte (`this.logger.` direto = 0 ocorrências, verificável por spec estrutural); no `catch`, corpo sempre string antes do `JSON.stringify` + redação; teste = 3 códigos do ORM ⇒ resolve com erro **e** 0 chamada externa
- N-036 [nestjs][jest][dev-backend][qa][passo-4][passo-6] `@Optional()` não satisfeito = `undefined` EM SILÊNCIO (escopo de módulo não é transitivo; import faltando desliga a feature com suíte verde — 441/441 com specs que instanciam via `new`, contornando o DI) — toda dep opcional nova ganha teste de FIAÇÃO que compila o módulo real e assere ≠ undefined; compile bloqueado por dep ESM sem transform ⇒ spec estrutural (comentários removidos) com causa verificada + limitação declarada (não pega ciclo/forwardRef); **reforço 21/08**: reincidiu 2× na mesma rodada — par mínimo `@Inject` explícito + `else` com `warn resultado=dependencia-ausente` + teste do CALL-SITE; o slug entra no grep das 48 h do runbook; **reforço 25/08**: variante FATAL — `@Optional() x?: Interface` sem `@Inject` num repositório derruba o bootstrap inteiro com 10 340 testes verdes; par `@Optional() @Inject(TOKEN)` obrigatório + guarda que compila o módulo (N-047) · **reforço 27/08 (4ª reincidência)**: boot do `dist` VERDE com o módulo fora dos 3 hospedeiros — `dependencies initialized` prova o módulo, não o hospedeiro; guarda = compilar CADA hospedeiro real + resolver o token + CONTROLE NEGATIVO executado; molde copiado traz o spec de fiação junto; comentário de aviso não é guarda · **reforço 27/08 (5ª reincidência) — regra ENDURECE**: injeção obrigatória é o default; `@Optional()` só quando o hospedeiro NÃO importa o provedor por desenho (com justificativa no CONTRACT) — se importa, é proibido e a correção é removê-lo; spec de fiação cobre TODO hospedeiro tocado pelo diff com controle negativo assertando o motivo; guarda de fonte remove comentários + controle do detector (falso positivo em JSDoc)
- N-037 [nestjs][prisma][jest][ops][passo-4][passo-6] Flag de banco "ausente = DESLIGADO": a semântica do default é comportamento e ganha spec no LEITOR REAL (`null`/`''`/`'N'`/lixo ⇒ off, mockar o gate = semântica nunca fixada) — prova = mutação da direção do default (`?? ''`→`?? 'S'` tem de avermelhar; sobreviveu com 510/510 verdes); leitor `findFirst` sem `ORDER BY` ⇒ go-live confere 1 linha viva da chave
- N-038 [nestjs][pm2][dotenv][ops][devops][passo-5] Env lida por dotenv/`ConfigModule` NÃO está em `/proc/<pid>/environ` (hash devolve sempre `e3b0c442` = vazio ⇒ loop de restart com env certa) e `sha256sum` de pipe hasheia o `\n` (`sha8` nunca bate) — prova do `pm2 restart` = anúncio no `onModuleInit` (`configurado=<bool> sha8=<8hex> causa=<slug>`) + `printf '%s' | shasum` do arquivo; `grep -c` no environ ⇒ 0 é o esperado; comando herdado de runbook declara o MECANISMO e é rodado contra o `dist/` antes de entrar (P-037); env por chamada ≠ troca a quente
- N-039 [nestjs][dev-backend][security][qa][passo-3][passo-4][passo-6] `ValidationPipe` GLOBAL pré-empta o local de `@Body` (global roda antes sobre parâmetro tipado): `forbidNonWhitelisted`/`exceptionFactory` do decorator = código morto no fio; envelope próprio só com corpo BRUTO + pipe chamado no handler (`expectedType`); `__proto__` só é apagado se o pipe validar aquele parâmetro ⇒ asserir efeito; ≥1 teste NO FIO; CONTRACT descreve envelope, não mecanismo
- N-040 [nestjs][rbac][security][qa][passo-4][passo-5] Spec de varredura RBAC por `prototype` torna helper `private` do controller um "handler sem permissão" (falso vermelho na guarda de segurança) ⇒ helpers como funções de MÓDULO; verbo por reverse lookup de `RequestMethod` (PATCH = 4), rota removida se prova como 404 do Nest
- N-041 [nestjs][ops][security][passo-5][passo-6] 403/401 NÃO passam por interceptor (guards antes): tabela de auditoria por interceptor mede uso AUTORIZADO, nunca tentativa barrada — runbook não pede "403 no log"; censo por auditoria exige controle positivo; 4xx de rota de escrita emite `warn` próprio
- N-042 [nestjs][jwt][security][dev-backend][qa][passo-3][passo-4] 2ª sessão JWT no MESMO emissor/segredo (portal × painel): guard antigo recusa o claim discriminador ANTES de qualquer I/O; guard novo exige `=== true` estrito + conjunto FECHADO de claims (sem `login`/`permissions`/`integrador`/`mfaPending`); escopo e `pwd_at` lidos do banco por request; teste de rejeição CRUZADA nos 2 sentidos; kill-switch na CLASSE do controller; RBAC interno fora (sem `@RequirePermission` é fail-open)
- N-043 [nestjs][dev-backend][qa][passo-5][passo-6] Enhancer por CLASSE em `@UseGuards`/`@UseInterceptors` resolve DI no módulo do CONTROLLER — exportar o enhancer sem os TOKENS que ele injeta passa em `build` e em 100% da unit (specs usam `new`) e quebra só o BOOTSTRAP; guarda = spec estrutural que lê o construtor e exige o token nos `exports` (removendo comentários antes de comparar, senão o mutante sobrevive); aceite é SUBIR a app, não buildar; **reforço 25/08**: mesma classe fora de enhancer (repositório, D-1) — "aceite é subir a app" virou gate executado 3× por 2 avaliadores; mecanismo em N-047

- N-044 [nestjs][dev-backend][qa][dev-frontend][passo-3][passo-4][passo-6] Exception filter global com ALLOWLIST de chaves do corpo de erro: campo novo do contrato de erro exige `throw` + allowlist (com guarda de tipo, allowlist continua fechada) — o teste do `throw` é do service, só teste por HTTP com o filtro real prova o fio (`total_calculado`/`data_fim` nunca chegavam ao front)
- N-045 [prisma][ops][qa][dev-backend][passo-3][passo-4][passo-5] Migration numerada por slot (`YYYYMMDD_NN_nome`) em árvore compartilhada: o slot andou `_03→_04→_05` numa rodada — spec localiza a pasta por GLOB DO SUFIXO + unicidade de prefixo (nunca `_NN` hardcoded), runbook/allowlists/CONTRACT citam o NOME, ordem por dependência declarada (`RAISE`), dev confere o slot AO CRIAR a pasta; não-contaminação provada varrendo diffs das suítes de migration
- N-046 [nestjs][cron][ops][dev-backend][passo-1][passo-3][passo-5] Cron novo confere a VIZINHANÇA do slot (`grep -rn "@Cron(" src` + integrações de cada vizinho): `0 9` já tinha 3 jobs, um no mesmo provider/OAuth ⇒ 429 e disputa de token lidos como "erro do job novo" — minuto vazio por provider (`15 9`), `{ name, timeZone }` explícitos (sem `timeZone` = UTC, 06:15 BRT), janela proibida de deploy DERIVADA do mapa de slots
- N-047 [nestjs][di][dev-backend][qa][devops][passo-5][passo-6] Parâmetro de construtor tipado por INTERFACE sem `@Inject(TOKEN)`: `design:paramtypes[i] === undefined` ⇒ `UnknownDependenciesException` SÓ no boot (build + unit com `new` verdes; `?` do TS ≠ `@Optional()`; `@Optional()` sem `@Inject` idem) — 3ª reincidência da classe, agora FATAL (derrubaria a API inteira no `pm2 restart`); guarda de fiação que COMPILA o módulo e assere por parâmetro (mutante: remover o `@Inject`), aceite = boot do `dist` por quem não escreveu; Prisma conecta preguiçosamente ⇒ boot sem banco; `perl -e 'alarm N; exec @ARGV'` (exit 142 = processo morto)
- N-048 [prisma][nestjs][dev-backend][qa][devops][passo-5][passo-6] Prisma `notIn: []` não exclui NADA: ramo de drift "fora da allowlist" com conjunto vazio se OMITE do `OR` (senão toda linha viva vira drift ⇒ `unsubscribe` em massa); predicado = a MESMA função do caminho síncrono sobre o catálogo (nunca SQL/`LIKE` sobre a coluna crua — normalização diverge), constantes de estado exportadas com complemento derivado; guarda AVALIA o `where` contra fixtures (forma deixa `notIn: []` passar); prévia SQL do runbook repete o predicado com `arquivo:linha` e cobre estados vivos + `data_exclusao IS NULL` · **reforço 26/08**: mesma classe no ramo de PENDENTES do job (2 dos 3 caminhos tinham a guarda); parâmetro ilegível degradado para `{}` também é vazio ⇒ inerte + `DEGRADADO`, nunca `∅` de negócio
- N-049 [prisma][nestjs][dev-backend][qa][devops][passo-3][passo-4][passo-5] Repositório com `db: any` + dublê de Prisma: chave inexistente em `select`/`where` (`data_exclusao` em tabela sem soft-delete; relação `locale_algar` × `locale`) passa em build e em 100 % da unit e só falha no BANCO (`PrismaClientValidationError` engolida ⇒ feature inerte com suíte verde) — conferir contra o bloco `model` do `schema.prisma`, tipar o cliente, 1 prova contra banco real por repositório novo; a presunção errada viaja para CONTRACT e SQL de runbook (`42703`)
- N-050 [nestjs][http][security][dev-backend][ops][passo-3][passo-6] URL vinda de parceiro (link, callback, download temporário) passa por allowlist de host EXATO (`https`, sem `user:pass@`) ANTES de `fetch`/persistência/e-mail/push — A-2/A-3 eram o mesmo defeito em 2 caminhos com a allowlist já existente e usada só no front; `redirect:'error'`, stream com teto que RECUSA, rota ao cliente nunca 302 (leva o `Authorization`); allowlist fail-closed cria slug de plantão (`host-nao-permitido`) com deploy como remédio — item de runbook + smoke; ampliar só com host exato, nunca sufixo (`amazonaws.com` reabre o SSRF); método morto com a URL crua se apaga na mesma correção
- N-051 [nestjs][jest][di][dev-backend][qa][passo-4][passo-6] Spec de fiação que compila módulos reais com `import` no topo + `require()` no caso carrega o arquivo do token 2× ⇒ 2 `Symbol()` ⇒ `Nest could not find Symbol(X)` INTERMITENTE sob workers (2 de 3 execuções; a suíte que piscava era a guarda do bloqueante) — forma de carga ÚNICA, compile memoizado por hospedeiro + `afterAll` fechando containers, 1 caso de identidade do token via `Reflect.getMetadata('providers')`; número oficial = paralela padrão SEM carga concorrente (`--runInBand` só confirma); "flake" vira achado só com isolado ×3 + paralelo sem carga
- N-052 [nestjs][prisma][security][dev-backend][qa][passo-3][passo-4][passo-6] Agregar relação por `include`/`select` traz ao processo TODAS as colunas da relação (credencial `link_assinatura`, PII) só para contar, sob perfil que só tem `read` do pai — agregação é `count`/`groupBy` por id (2 consultas, sem N+1) e o repositório tem teste de LISTA NEGATIVA de relações sensíveis; Security nomeia no CONTRACT as relações "só contáveis"

## Stack: React + Vite (stacks/react-vite.md)

- R-001 [react][dev-frontend] Flag de feedback visual transitório (`setTimeout` que volta em Ns) nunca serve de flag de permissão de fluxo — dois estados: permanente (`jaCopiou`) × transitório (`mostrandoCheck`); reusar um pelo outro re-desabilita o botão/re-bloqueia o dialog
- R-003 [react][dev-frontend][qa] Campo não-opcional vindo de API é promessa, não garantia (`data.acoes.length` ⇒ TypeError na tela inteira): tipo do front declara coleção de API como OPCIONAL, normalização pura no SERVIÇO (`?? []` + alias) e `?? []` também no componente; cuidado com R-002 quando o vazio afirma
- R-002 [react][dev-frontend][qa] Estado vazio que AFIRMA ("sem X") ≠ dado ausente: renderização distingue `undefined`/campo omitido (gate OFF, falha — célula neutra) de `[]` real (afirmação); backend coopera omitindo o campo na degradação, nunca `[]` fabricado
- R-004 [react][dev-frontend][qa] "Carregando" ≠ "indisponível": catálogo remoto em voo renderizado como falha aparece em todo 1º render e treina o operador a ignorar alerta — estado derivado **puro** de 3 valores (`carregado|carregando|indisponivel`, refetch com dado presente continua carregado), alerta de falha atrás do gate `indisponivel`, e o que já é verdade sem o catálogo (a ordem) continua visível
- R-005 [react][dev-frontend][qa][a11y] Foco programático após mover/remover item: `getElementById(...)?.focus()` é **no-op silencioso** sem `id` no alvo e `focus()` em elemento `disabled` não faz nada — decisão do alvo em função **pura** (casos de ponta: 1ª/última linha, lista de 1, lista vazia), `id` explícito em todo alvo possível, **cadeia de fallback** que pula `disabled`; foco real/`aria-live`/contraste **não são testáveis sem jsdom** ⇒ vão para o roteiro manual, não para uma asserção de mentira
- R-006 [react][dev-frontend][qa] Cliente que corta lista em N pede N+1 (sentinela) ou usa o `total` — `pageSize: TETO` torna o detector de truncagem INALCANÇÁVEL e o corte silencioso; o teste assere o que o hook PEDE à API, não só a função pura de corte
- R-007 [react][react-query][dev-frontend][qa] `QueryClient` sem `defaultOptions` refaz chamada PAGA em foco/mount/reconnect — query de integração paga espalha constante anti-refetch compartilhada, assertada POR hook (J-007); grep dos hooks existentes: o defeito costuma já estar em produção
- R-008 [react][dev-frontend][ux-ui][qa][a11y][passo-2][passo-6] Foco pós-ação cobre o desmonte do CONTAINER, não só do controle: alvo dentro do card some com ele e o foco cai no `<body>` — matriz de estados declara alvo de fallback FORA da seção (âncora da página com `tabIndex={-1}`), função pura devolve `alvo | null` por desfecho, CONTRACT ganha o caso puro + alínea manual "foco após o card sumir" · **reforço 26/08**: 2ª implementação (`#stations-titulo` × `#linha-titulo`), live region limpa por timer > toast com `clearTimeout` e hook antes do `return null`
- R-009 [react][dev-frontend][ux-ui][qa][passo-6] Título e corpo/CTA derivados do MESMO estado por normalizações diferentes se contradizem (`"assinado"` minúsculo ⇒ título de um estado, corpo de outro) — UMA função pura devolve `{titulo, corpo, cta, tier}` do estado normalizado, teste de COERÊNCIA sobre a tabela de estados (caixa/espaços/ausente/desconhecido), ação indisponível AUSENTE (nunca `disabled`) e `aria-label` uma vez, tier de diagnóstico ≠ tier visual; valor-credencial (link) buscado no clique com `gcTime: 0` → clipboard, nunca DOM/estado/log · **reforço 27/08**: mesma classe num NÚMERO — "Vence hoje" (linha) × "Vence em 0 dias" (alerta) do mesmo `dias_restantes`; teste varre estados × valores provando que a substring proibida não sai de NENHUMA peça; botão que abre dialog dependente de dado em voo SOME enquanto carrega
- R-010 [react][dev-frontend][ux-ui][qa][a11y][passo-2][passo-6] Copy que ENUMERA causas se confere contra o conjunto FECHADO de causas do backend (faltava a mais provável em produção — teste de cobertura causa×copy, não de string; a mais frequente vem primeiro); tooltip/dica com trigger não focável é inalcançável por teclado — `tabIndex={0}`/`<button>` + `aria-describedby`

## Stack: Next.js (stacks/nextjs.md)

- NX-001 [nextjs][security][dev-frontend][passo-6] `?redirect=` honrado sem validação é open redirect e a guarda é POR CALL-SITE: 2º ponto de entrada (registro) nasceu sem o `safeRedirect` do 1º (login) — todo `router.push` com valor de query passa pela guarda; criar a guarda inclui varrer TODOS os call-sites do param
- NX-002 [nextjs][sentry][dev-frontend][qa][passo-4][passo-6] `beforeSend` que devolve `{}` ENVIA evento vazio (só `null` descarta) — scrub de evento devolve `null` p/ não-normalizável e preserva `exception`; o teste EXECUTA o `beforeSend` dos 3 configs com evento realista (checar import por string é falso-verde)

## Stack: Python/FastAPI (stacks/python-fastapi.md)

## Stack: Spring Boot (stacks/spring-boot.md)
