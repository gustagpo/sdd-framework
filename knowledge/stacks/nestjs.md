# Lições de Stack — NestJS

> Lições genéricas desta stack aprendidas em rodadas SDD (qualquer projeto). Complementam o perfil `standards/stacks/nestjs.md` — se uma lição contradiz o perfil, corrija o perfil. Cada lição tem 1 linha no `knowledge/INDEX.md`.

### N-001 — Jest 30: flag `--testPathPatterns` (plural)
**Contexto**: rodar uma suíte isolada no Jest 30.
**Problema**: `--testPathPattern` (singular) foi removido no Jest 30 e aborta a execução com "Unrecognized CLI Parameter".
**Regra**: use sempre a forma plural `--testPathPatterns="..."`. Atualize scripts/exemplos herdados do Jest ≤29.
**Origem**: algar/LESSONS.md L019

### N-002 — `Logger` não declarado em repositório trava o watch mode
**Contexto**: usar `this.logger` numa classe `*RepositoryImp` (ou qualquer provider) sem declarar a propriedade.
**Problema**: gera `TS2339: Property 'logger' does not exist` que impede a recompilação no watch mode — o servidor segue rodando o código antigo sem recarregar, mascarando a mudança.
**Regra**: antes de usar `this.logger`, importar `Logger` de `@nestjs/common` e declarar `private readonly logger = new Logger(Classe.name)`.
**Origem**: algar/LESSONS.md L004

### N-003 — `console.log`/`util.inspect` trunca objetos aninhados
**Contexto**: logar um objeto de diagnóstico (payload de webhook, resposta de provider) em produção.
**Problema**: o Node usa `util.inspect` com profundidade 2 no `console.log` — níveis mais fundos viram `[Object]`/`[Array]` e o diagnóstico se perde.
**Regra**: nunca `console.log(objeto)` em produção. Use o `Logger` do Nest com `JSON.stringify(obj, null, 2)` para qualquer objeto aninhado importante.
**Origem**: algar/LESSONS.md L005

### N-004 — Cast TypeScript não converte em runtime
**Contexto**: uma coluna do banco tem tipo (ex.: `Char(1)`) diferente do tipo exposto no domínio (ex.: `boolean`).
**Problema**: `as unknown as string` / `as unknown as boolean` só engana o compilador — em runtime o valor continua o mesmo, e o ORM rejeita (ex.: Prisma `Expected String or Null, provided Boolean`). Mocks de repositório em Jest não pegam isso.
**Regra**: faça a conversão explícita **no repositório** (gravar/ler/filtrar convertendo o valor de verdade), nunca via cast de tipo.
**Origem**: algar/LESSONS.md L003

### N-005 — Cliente Prisma gerado defasado (stale) derruba specs não relacionados
**Contexto**: `schema.prisma` mudou (colunas/tabelas novas) e ainda não se rodou o `db:generate`.
**Problema**: o `ts-jest` type-checa contra o cliente gerado em disco (`generated/prisma`). Um cliente stale derruba specs fortemente tipados de modelos **não relacionados** à mudança, com "Test suite failed to run" + erro de tipo do Prisma — parece regressão da feature, mas é dívida de ferramenta.
**Regra**: ao ver esse sintoma numa suíte não relacionada, suspeite de cliente stale e rode o `db:generate` do projeto **antes** de medir baseline ou creditar a falha.
**Origem**: algar/LESSONS.md L039

### N-006 — Jest com mocks não pega erro de tipo do Prisma — rodar `build`, não só `test`
**Contexto**: backend implementado e suíte Jest 100% verde.
**Problema**: specs mockam o repositório inteiro com `jest.fn()` e **não** exercitam os tipos do cliente Prisma real. Erros como `@@unique(map:)` que não gera accessor TS (`WhereUniqueInput` sem a chave da constraint), ou qualquer `TS2353`, passam despercebidos no `test` e só aparecem no `build`.
**Regra**: após o backend, rode o `build` do projeto (não apenas `test`) para type-checar contra o cliente Prisma gerado. Para duplicata de constraint composta, `findFirst` pelos campos individuais em vez de `findUnique` pelo nome da constraint. (Distinto de N-005: lá o cliente está stale; aqui os mocks simplesmente não type-checam.)
**Origem**: algar/LESSONS.md L016

### N-007 — N+1 insert dentro de `$transaction` interativa estoura o timeout (P2028)
**Contexto**: fluxo de lote (centenas de linhas) que grava por item dentro de um `$transaction` interativo do Prisma.
**Problema**: um `create` por item em loop `for...await` são N round-trips sequenciais; o timeout default da transação interativa é **5000 ms** e um lote grande estoura antes de terminar (`P2028 — transaction ... expired`), revertendo tudo. O unit test com mock não detecta — round-trip não existe no mock.
**Regra**: em lote dentro de transação, toda escrita repetida por item vira `createMany`/`updateMany` (uma query por tabela). O `timeout` explícito é salvaguarda, nunca a correção — subir o timeout sem remover o N+1 só adia o estouro.
**Origem**: algar/LESSONS.md L041

### N-008 — Efeito colateral externo é pós-commit; auditoria acoplada ao estado é intra-transação
**Contexto**: uma operação transacional precisa tanto disparar efeitos externos quanto registrar auditoria.
**Problema**: confundir os dois. Chamada externa (cobrança, notificação, HTTP a provider) **dentro** do `$transaction` segura a conexão e pode reverter um efeito já materializado no mundo externo. Já uma trilha de auditoria acoplada à mudança de estado, se gravada best-effort **fora** da transação, dessincroniza do estado quando a operação falha no meio.
**Regra**: efeitos externos/irreversíveis rodam **pós-commit**, fora da transação, com captura de erro best-effort. Auditoria acoplada à mudança de estado entra **na mesma transação** (atômica), concentrada num helper tx-aware que recebe o `tx` — sem repositório-em-repositório.
**Origem**: algar/LESSONS.md L040

### N-009 — `ValidationPipe` global sem `transform` não converte DTO de query
**Contexto**: DTO de filtro de query com `@Type(() => Number)` / `@IsInt()` sob um `ValidationPipe` global registrado sem `transform: true`.
**Problema**: `@Type(() => Number)` (class-transformer) só roda com `transform` ligado. Sem ele, a querystring chega como string — `@IsInt()` rejeita com 400, ou a string vaza para o `where` do ORM.
**Regra**: não assuma `transform` global. Use um `ValidationPipe` **local** com `transform: true` no `@Query(...)`, ou converta manualmente (`Number(query.x)`) no controller.
**Origem**: algar/LESSONS.md L021

### N-010 — Método transacional gigante não é unitariamente testável — extrair a lógica pura
**Contexto**: uma regra de negócio (decisão/mapeamento/cálculo) vive dentro de um método de orquestração transacional de centenas de linhas.
**Problema**: para testar 1 regra seria preciso mockar todo o método transacional — mock frágil e caro; na prática a regra fica sem teste unitário.
**Regra**: extraia a lógica pura para um `*.util.ts` com spec próprio (funções sem I/O). Fallback aceitável só para mudança trivial de baixo risco enquanto não extraído: `build` verde + inspeção do diff + verificação manual — registrando a dívida.
**Origem**: algar/LESSONS.md L020

### N-011 — Escrita externa com custo real: validar invariantes e pré-persistir o id de idempotência NA transação; provider pós-commit
**Contexto**: fluxo que dispara uma operação externa irreversível/com custo (estorno, cobrança, postagem) condicionada a uma invariante de negócio (saldo, estado, limite).
**Problema**: validar a invariante fora da transação permite double-submit (duas requisições passam pelo check e chamam o provider 2×); validar mas chamar o provider **dentro** da transação viola N-008 (conexão presa + efeito externo não-revertível dentro de rollback); e confirmar o estado local antes da resposta real do provider cria "confirmado fantasma" quando o upstream falha.
**Regra**: sequência canônica — (1) `$transaction` + advisory lock por chave de negócio (`pg_advisory_xact_lock`); (2) **recomputar** a invariante dentro da tx (nunca confiar no valor lido antes) e rejeitar na borda com **zero** chamada externa; (3) **pré-persistir** o registro da operação com id de idempotência gerado localmente (o provider recebe o mesmo id em retry); (4) commit; (5) chamar o provider **pós-commit** e só então transicionar o estado com a resposta real — falha upstream vira erro tipado, nunca estado confirmado. Testar com duas chamadas concorrentes assertando 1 única chamada externa, e com valor inválido assertando ausência total de chamada.
**Origem**: algar/LESSONS.md L065 + L056 (rodadas integracao-correios e pagamentos-auditoria-estorno, 07/2026)

### N-012 — Migration que depende de linha/constraint pré-existente vira no-op silencioso — asserir a pré-condição
**Contexto**: migration SQL idempotente (Postgres/Prisma) que concede RBAC, semeia domínio ou copia dados usando `INSERT ... SELECT FROM <tabela>` ou `INSERT ... ON CONFLICT DO NOTHING`.
**Problema**: dois modos de falha **silenciosos** — (a) `INSERT ... SELECT` cujo `SELECT` não encontra a linha de origem (tela/perfil/domínio esperado não existe naquele ambiente) insere **zero linhas e termina com sucesso**; (b) `ON CONFLICT` exige uma constraint **UNIQUE** correspondente — sem ela o Postgres erra (ou, com `WHERE NOT EXISTS` mal escrito, duplica). O ambiente fica meio-migrado sem nenhum erro no log e o furo só aparece em runtime (403 de RBAC, lookup vazio).
**Regra**: todo bloco de migration dependente de estado pré-existente ganha (1) **queries de pré-requisito documentadas** no roteiro de operação (rodar antes de aplicar: a tela/domínio/perfil existe?), (2) **queries de conferência pós-aplicação** (contagens esperadas), e — quando o projeto testa migrations — um spec que guarda a pré-condição. `ON CONFLICT` só com a UNIQUE conferida no schema real; senão `WHERE NOT EXISTS`.
**Origem**: algar/LESSONS.md DO-08/DO-07 da rodada emissao-nfe-produto (22/07/2026) + L048/L063 (migrations de correção e RBAC fail-closed)

**Reforço (27/08/2026, algar, ciclo-vida-contratual — a variante RUIDOSA da mesma classe)**: a migration `20260827_01` semeava 3 **colunas** (`email_template.conteudo_html`, `evento_notificacao.propriedades`, `.cod_email_template`) nascidas em 3 migrations **pendentes** — ausentes do dump de produção. Aqui não há no-op: é `42703 undefined column` e, sob `--single-transaction`, **rollback total** com o deploy do backend travado atrás (ordem dura migration → app). Passou por CONTRACT, 2 specs de migration e 189 casos: nenhum pergunta "a coluna existe no ALVO?"; só o DevOps, lendo o `COPY` do dump, viu. Regra estendida: (4) toda coluna citada em seed declara **em qual migration nasce** (cabeçalho do `.sql`) e o runbook ganha **sonda de pré-requisito de schema** (`information_schema.columns`, aqui P1c = `1/1/1`) + degrau "aplicar as pendentes, 2× cada, antes"; (5) o spec de migration pode extrair as colunas citadas e cruzá-las com o DDL dos dumps locais (extensão de N-019) — em projeto com fila de migrations pendentes por ambiente é a única guarda estática possível. L182 do projeto.

### N-013 — Hash de segredo de alta entropia não é hash de senha: SHA-256 puro, e o prefixo de algoritmo desambigua
**Contexto**: persistir com segurança um segredo de máquina gerado pelo servidor (`crypto.randomBytes(32)` → 256 bits: token de bypass, API key, webhook secret) do qual só se precisa verificar **igualdade**.
**Problema**: aplicar o reflexo "senha → bcrypt/argon2" aqui é desperdício e às vezes quebra o requisito. Uma KDF lenta existe para conter brute-force de segredos de **baixa** entropia (senhas humanas); para 256 bits aleatórios não há brute-force viável, e a KDF adiciona ~100 ms **por login** sem reduzir risco. Pior: **salt** (inerente a bcrypt/argon2) inviabiliza qualquer comparação por igualdade e qualquer backfill/migração determinística. E, ao migrar a coluna de texto plano para hash, não há como o código distinguir um valor já-hasheado de um legado em claro.
**Regra**: para segredo de alta entropia gerado pela máquina, use **hash rápido sem salt** (SHA-256) e comparação **timing-safe** (`crypto.timingSafeEqual` sobre os digests). Prefixe o valor persistido com o **algoritmo** (`sha256:<hex>`): o prefixo (1) torna a migration idempotente (`WHERE valor NOT LIKE 'sha256:%'`), (2) deixa o login **fail-closed** sobre qualquer resquício de texto plano, e (3) permite evoluir o algoritmo depois. Se algum dia o segredo puder ser **escolhido por humano**, a decisão se inverte (volta a KDF+salt).
**Origem**: algar/LESSONS.md L072 + CONTRACT §10 da rodada bypass-seguranca-integrador (22/07/2026)

### N-014 — Parâmetro opcional numa guarda de autorização fura a regra em silêncio
**Contexto**: um service aplica uma regra de segurança condicionada a um argumento (ex.: `generateBypassSecret(id, codUsuarioLogado?)` que proíbe auto-emissão com `codUsuarioLogado !== undefined && codUsuarioLogado === id → 403`).
**Problema**: com o parâmetro **opcional**, um segundo call-site que esqueça de passá-lo faz a condição `!== undefined` curto-circuitar para **falso** e a guarda **não dispara** — fail-open silencioso, sem erro de compilação nem de teste (o call-site coberto passa). A regra existe no código e mesmo assim não protege.
**Regra**: quando uma regra de segurança/autorização depende de um argumento, esse argumento é **obrigatório** na assinatura (o compilador força todo call-site a fornecê-lo). Nunca `x !== undefined && <regra>` numa guarda — a ausência do dado deve **falhar fechado**, não pular a checagem. Escreva um teste que **omite** o argumento e assere o bloqueio.
**Origem**: algar/EVALUATION R-02/S-05 da rodada bypass-seguranca-integrador (22/07/2026)

### N-015 — Tabela de lookup compartilhada com labels homônimas: resolvedor simétrico entre leitura e escrita (`escrita ⊆ leitura`), e fixture com o homônimo hostil
**Contexto**: projetos com tabela de domínio/lookup genérica (ex.: `itemdominio` com `descricao` + FK para o domínio) onde **a mesma label** (`PENDENTE`, `ENVIADA`, `DISPONÍVEL`) existe em vários domínios, e uma guarda de idempotência/estado decide por `IN (ids resolvidos)` se dispara um efeito com custo real.
**Problema**: endurecer o resolvedor (**escopar por domínio**) **só na leitura** da guarda, deixando as escritas com `findFirst({ descricao })` solto, cria uma janela fail-**open**: a escrita pode gravar o id homônimo de **outro** domínio, que fica fora do `IN` da guarda — a guarda "não vê" a linha existente e o efeito com custo (compra, cobrança, envio) **repete indefinidamente**. O código 100% solto era imune (casava qualquer homônimo dos dois lados); o endurecimento parcial **introduz** a regressão. A suíte não pega: mocks que devolvem o esperado e fixtures que só modelam o domínio correto passam com o bug.
**Regra**: leitura e escrita do mesmo estado usam o **mesmo resolvedor escopado** — e, em código com histórico de escrita solta (linhas legadas no banco), a leitura da guarda vira a **união** escopado ∪ solto (mais ids = mais bloqueio = fail-closed), garantindo `escrita ⊆ leitura` **por construção**; resolvedor sem resultado ⇒ **bloqueia** (nunca libera). No teste, um **fake do ORM que honra o `where`** (não mock de retorno fixo) com o **homônimo de outro domínio posicionado antes** na fixture (o que um `findFirst` solto pegaria) é o único jeito de reproduzir o bug; asserir literalmente que o id gravado pela escrita pertence ao `IN` que a guarda lê. Ao endurecer lookup legado, endureça os dois lados **na mesma mudança**.
**Origem**: algar/EVALUATION D-01 (ALTA/bloqueante) da rodada solicitacao-chip-por-estoque (23/07/2026) + algar/LESSONS.md L074 (parente da L042)

### N-016 — Leitura de domínio que lança `NotFoundException` com o id na mensagem não pode ser reusada crua em borda pública anti-enumeração
**Contexto**: rota pública (checkout/portal) que deve responder 404 genérico **byte-idêntico** para qualquer ref inválido/inexistente (anti-oráculo), reusando um service de domínio escrito para o painel interno autenticado.
**Problema**: o service interno lança `NotFoundException("Entidade <id> não encontrada")` — ecoa o id e diverge do 404 genérico, criando um **oráculo de existência**. Pior: o `if (!x) throw new NotFoundException(<genérico>)` escrito depois da chamada é **código morto** (a exceção do domínio já subiu) e dá falsa sensação de cobertura. O furo escapa quando o teste de 404 só cobre a rota nova (GET) e as rotas de escrita reusam a leitura antiga por caminho próprio.
**Regra**: na borda pública, envolva a leitura de domínio num wrapper que **captura o `NotFoundException` e relança o genérico** (demais erros propagam). O teste anti-oráculo é **comparativo e por rota**: todas as combinações rota × tipo de ref (numérico/opaco/malformado × existente/inexistente) numa única asserção `Set(JSON.stringify({status, body})).size === 1` + ausência do ref no corpo — nunca apenas "retornou 404". Checklist prévio: toda leitura de domínio reusada em rota pública tem contrato de erro verificado (**lança** ou **devolve null**?).
**Origem**: algar/LESSONS.md L076 — rodada checkout-token-opaco-e-logistica (23/07/2026); it. 1 reprovada no Passo 6 por 404 distinguível nos POST públicos, achado por QA e Security independentemente

### N-017 — Update parcial sem DTO: decidir o ramo de validação pelo tipo do PAYLOAD é bypass — resolver o estado efetivo (`payload ?? banco`)
**Contexto**: endpoint de `update` legado sem DTO (`body: any`) cuja validação de domínio depende de um discriminador do registro (tipo, categoria, estado) que o payload **pode omitir**; Prisma (e ORMs em geral) ignoram `undefined` no update.
**Problema**: decidir o ramo de validação pelo campo do payload (`if (payload.tipo === X) validar()`) faz o `PUT` **parcial** escapar da regra inteira: o campo omitido curto-circuita a condição, o registro mantém o discriminador antigo no banco e os demais campos do payload persistem sem validação — bypass silencioso, comprovável por exploit (rodada real: preço proibido persistido num serviço de tipo travado, S-01). O `create` não sofre disso (payload completo), o que esconde o furo na revisão.
**Regra**: em update sem DTO, a decisão de ramo é sempre sobre o **estado efetivo** — `payload.discriminador ?? (await lerDiscriminadorAtual(id))` — custe uma leitura a mais; e o teste do exploit (`PUT` parcial num registro do tipo travado ⇒ 400, repositório **não** chamado) entra na bateria junto com o de não-regressão (parcial em registro de tipo livre segue gravando). Isso **não substitui** criar o DTO — apenas fecha o bypass enquanto o débito existir.
**Origem**: algar/LESSONS.md L086 — achado S-01 (Security, bloqueante) da rodada servico-bonus-temporario (28/07/2026); exploit reprovado na revalidação.

**Reforço (27/08/2026, algar, ciclo-vida-contratual — S-3, com DTO)**: a classe não depende de `body: any` — com `UpdateContratoDto` validado, o PUT parcial `{data_fim_vigencia}` era comparado só com o `data_inicio` **do payload** (ausente ⇒ sem comparação) e aceitava período invertido contra o `data_inicio` **gravado** ⇒ `EXPIRADO` ⇒ e-mail interno na madrugada. Mesma regra: coerência entre campos de um update parcial se decide sobre `payload ?? banco`, **dentro** da transação (`throw` ⇒ 400, 0 coluna, 0 evento), e só quando o PUT toca o campo em questão (editar `renovacao_automatica` isolado segue 200); regra pura `periodoInvertido()` + T-076 (9 casos incl. limites). L187 do projeto.

### N-018 — Migration de rename/correção de valores exibidos: alvo por JOIN + igualdade estrita — `_` é curinga em `LIKE`, e nem toda tabela tem colunas de auditoria
**Contexto**: migration SQL (Postgres/Prisma) que renomeia valores de lookup/catálogo (descrições de domínio, nomes de tela/funcionalidade) exibidos em UI, com módulos vizinhos de prefixo parecido que NÃO devem mudar.
**Problema**: dois reflexos que parecem seguros e não são — (a) mirar por `LIKE 'PREFIXO_%'`: o `_` é curinga de **1 caractere** em `LIKE`, então `'PLANO_%'` casa `PLANO_LOGISTICA_*` e arrasta linhas de outro módulo (no banco real: 9 linhas com o prefixo, só 6 eram alvo); (b) carimbar auditoria (`usuario_alteracao`/`data_alteracao`) por padrão do projeto: se a tabela-alvo **não tem** as colunas, a migration quebra com `42703` — o padrão "auditoria em toda tabela" não vale para todo o catálogo.
**Regra**: alvo resolvido por **JOIN nas chaves estáveis + igualdade estrita no valor antigo** (⇒ idempotência: rerun = 0 linhas); quando há vizinho homônimo, **âncora redundante** (JOIN pelo código-pai + lista explícita de códigos + valor antigo — qualquer uma isolada já exclui o vizinho); **zero** `LIKE`/`SIMILAR TO`/`~` no corpo executável (se `LIKE` for inevitável, escapar `\_`); conferir **no schema** se cada tabela escrita tem as colunas de auditoria antes de carimbá-las (e o spec assere a assimetria); rollback = bloco **inerte** comentado com os valores antigos.
**Origem**: algar/LESSONS.md L048 (atualizada) + L088 — rodada renomeacao-plano-para-assinatura (28/07/2026); migration `20260728_02`, 7 UPDATEs/12 linhas, `PLANO_LOGISTICA` intocado por construção.

### N-019 — Spec de migration executável: ler o `.sql` real e tokenizar os comandos — prosa/grep manual não protege
**Contexto**: projetos que testam migrations com Jest (sem Postgres no CI) e contratos que exigem garantias sobre o SQL (colunas gravadas, larguras, idempotência, escopo de tabelas).
**Problema**: caso de teste que **descreve** o SQL em prosa ("o UPDATE deve…") ou checklist de grep manual não falha quando a migration muda; e regex sobre o arquivo inteiro não distingue corpo executável de comentário/bloco de rollback inerte — as guardas viram ritual.
**Regra**: o spec faz `readFileSync` do `.sql`, **remove comentários** e **tokeniza os comandos** (isolando a cláusula `SET` de cada `UPDATE`). Asserções executáveis: (1) **whitelist** das colunas à esquerda de `=` em `SET` (nenhuma coluna de gating/chave — `codigo`, `rota`, `tabela`, `campo`); (2) todo literal que entra em `varchar(N)` com `[...str].length ≤ N` (**caracteres**, não bytes — acento é multibyte); (3) todo `WHERE` casa o valor **antigo** (rerun = 0); (4) conjunto de tabelas escritas ⊆ esperado; (5) padrões proibidos (`LIKE`, DDL, `DELETE`) ausentes do corpo executável; (6) rollback presente **e 100% comentado**. Rodada real: 99 testes/1,3s substituíram o checklist manual do contrato. A validação continua **estática** — o gate empírico segue sendo aplicar em staging (rows-affected + rerun, ver N-012).
**Origem**: algar/LESSONS.md L080 (padrão evoluído) — rodada renomeacao-plano-para-assinatura (28/07/2026); `renomeacao-plano-para-assinatura.migration.spec.ts`.

### N-020 — FK no Prisma sem **relação declarada** devolve só o id: o objeto que o consumidor espera chega `undefined` em silêncio
**Contexto**: tabela de configuração cujas colunas são FKs (`cod_email_template`, `cod_evento_notificacao`) e cujo consumidor precisa do **conteúdo** referenciado (o alias do template, a chave do evento), não do id.
**Problema**: no `schema.prisma` é perfeitamente válido declarar só a coluna escalar (`cod_email_template Int?`) sem o campo de relação. O `findMany` então **não aceita `include`** daquele nome — e o mapeador que tenta ler `row.email_template.alias` recebe `undefined`. Nada explode: o serviço a jusante segue com o campo vazio e degrada pelo caminho de erro (canal `IGNORADA`/`ERRO`), o que parece funcionamento normal em painel e log. Em rodada real isso deixou **todos os canais de notificação inertes** — e um cast de asserção no mapeador (`as Config[]`) impediu o compilador de apontar a discrepância (ver N-004 e a regra de tipar pelo produtor).
**Regra**: (1) toda FK cujo consumidor espera o **objeto** ganha campo de relação declarado nos **dois** modelos + `include` explícito na consulta — `prisma validate` confirma; (2) o mapeador é tipado pelo **retorno real** da consulta (nunca `as`), de modo que remover o `include` vire erro de compilação; (3) o teste que cobre a leitura parte da **linha crua** (o objeto que o Prisma devolveria, com a relação aninhada) e chega ao consumidor final — fixture montada no shape que o serviço deseja não prova nada sobre a consulta (J-003).
**Origem**: algar/LESSONS.md L090 — BLOQ-1 da rodada esteira-cobranca-inadimplencia (31/07/2026); a feature inteira era inerte com 322 testes verdes no recorte.

### N-021 — Guarda de constraint por `conname` sem `conrelid` falha aberta: o nome de constraint é único POR RELAÇÃO, não global
**Contexto**: migration idempotente (Postgres) que cria FK/constraint dentro de `DO $$ … IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='fk_x') … $$` — o padrão canônico para `ADD CONSTRAINT` que não tem `IF NOT EXISTS` nativo.
**Problema**: `pg_constraint.conname` é único **por relação** (`conrelid`), não no banco — dois `fk_pedido` podem coexistir em tabelas diferentes. A guarda que filtra só por `conname` encontra o **homônimo de outra tabela**, conclui "já existe" e **pula a criação da FK em silêncio**: a migration reporta sucesso, a conferência por existência (o mesmo `SELECT`) também passa, e a integridade referencial simplesmente não existe. É o modo de falha mais traiçoeiro de N-012 (no-op silencioso) porque a própria verificação está viciada pelo mesmo predicado.
**Regra**: toda guarda (e toda conferência pós-aplicação) sobre `pg_constraint` filtra por **`conname` E `conrelid = '<tabela>'::regclass`**; o mesmo raciocínio vale para qualquer catálogo com unicidade escopada (`pg_indexes` já embute `tablename` — usar; triggers via `tgrelid`). A conferência por valor confere adicionalmente `confrelid` (a tabela **referenciada**) — nome certo apontando para a tabela errada também é falha silenciosa.
**Origem**: algar, rodada contrato-documento-d4sign (04/08/2026) — finding F-5 do DevOps, corrigido na it.2 nas duas guardas de FK da migration `20260804_01`.

### N-022 — Sonda de "abre transação própria?" por grep de `begin|commit` acusa falso positivo em `DO $$ BEGIN`: decide errado o `--single-transaction`
**Contexto**: runbook que decide se um `.sql` de migration deve ser aplicado com `psql --single-transaction` (o certo quando o arquivo NÃO abre transação própria) usando uma sonda automatizada tipo `grep -Ei '^\s*(begin|commit)'` sobre o arquivo.
**Problema**: todo bloco `DO $$ BEGIN … END $$` (o padrão de guarda idempotente de constraint — N-021) começa a linha com `BEGIN` — que é o início do bloco **PL/pgSQL**, não um `BEGIN` de transação SQL. A sonda acusa "o arquivo já gerencia transação" e induz o operador a aplicar **sem** `--single-transaction`: uma falha no meio do arquivo deixa a migration **pela metade** (metade dos domínios/FKs aplicada), exatamente o cenário que a atomicidade evitaria — e o rerun idempotente mascara o vestígio.
**Regra**: a sonda distingue os contextos — `BEGIN` de transação aparece **fora** de blocos `$$…$$` (e tipicamente como `BEGIN;`/`BEGIN TRANSACTION`); na prática: remover os blocos dollar-quoted antes do grep, ou casar apenas `^\s*BEGIN\s*(;|TRANSACTION|WORK)`. Em caso de dúvida, a decisão default é **com** `--single-transaction` + `-v ON_ERROR_STOP=1` (um `BEGIN` aninhado gera warning inócuo; migration meio-aplicada gera incidente). O runbook registra a decisão e o porquê, nunca só o comando.
**Origem**: algar, rodada contrato-documento-d4sign (04/08/2026) — finding F-1 do DevOps, corrigido no RUNBOOK.md; a migration era `DO $$`-pesada e o falso positivo era certo.

### N-023 — Idempotência conferida antes do lock é corrida aberta — e o teste que a prova exige transações genuinamente simultâneas
**Contexto**: função de banco (plpgsql) ou serviço que implementa efeito idempotente com custo real (débito de créditos, cobrança, contador) no padrão "se `idempotency_key` já existe, retorna ok sem efeito".
**Problema**: checar a key (`EXISTS`) **antes** de tomar o lock da linha-alvo (`SELECT … FOR UPDATE`) é idempotência falsa: duas transações simultâneas com a mesma key passam juntas pelo `EXISTS` (nenhuma vê a outra, que ainda não commitou) e o efeito duplica. O agravante é que o teste clássico ("chama 2×, espera 1 efeito") roda **em série** e passa para sempre contra o código quebrado — foi exatamente assim que um débito duplicado de créditos atravessou uma fase inteira com suíte verde.
**Regra**: (1) a checagem de idempotência vem **depois** do lock que o próprio efeito já toma (serializar por ele custa uma linha e fecha a corrida; a `UNIQUE (scope, idempotency_key)` continua como cinto de segurança); (2) o teste que prova concorrência dispara T2 com a transação de T1 **aberta e segurando o lock** — senão é teste sequencial disfarçado; asserir o estado final **e** a contagem de linhas do efeito (saldo certo com 2 linhas na razão ainda é bug); cronometrar ajuda a provar que T2 esperou a janela; (3) na revalidação, rodar a forma do teste contra a versão **antiga** da função — se não falha contra ela, o teste não prova nada.
**Origem**: iara, rodada 001-auditoria-fase-0 (12/08/2026) — achado ALTA F0-03-c (`fn_debit_credits`/`fn_refund_credits`, `0006`→`0012`); sonda diferencial no EVALUATION §2; lição L001 do projeto.

**Reforço (27/08/2026, algar, ciclo-vida-contratual — Prisma `updateMany`, sem plpgsql)**: duas formas da mesma corrida. (a) **Marcador condicionado só por PK + marcador**: `marcarAviso`/`rolarDataBase` faziam `updateMany({ where: { cod_objeto, marco: null } })` — uma renovação **entre** a listagem e o update regravava o marcador do período **antigo** sobre o **novo** e o contrato **nunca mais** era avisado (S-1, silencioso e permanente). Fix: o `where` repete a **ponta de dado lida** (`data_fim_vigencia` do `SELECT`) + predicado de elegibilidade compartilhado; `count=0` = perdeu a corrida ⇒ no-op sem efeito externo; dado nulo ⇒ recusa sem query. (b) **Lock que relê mas não revalida**: `pg_advisory_xact_lock` + releitura + escrita incondicional ⇒ 2 renovações concorrentes não duplicam o período mas geram **2 eventos e 2 e-mails** ("de X para X"); a releitura tem de **decidir** (já no estado pedido ⇒ `duplicada`, 0 update, 0 evento, devolve o evento do vencedor) e o serviço não dispara. Teste: `where` asserido campo a campo (T-070/71/72), `count=0 ⇒ false`, lock **antes** da releitura, `duplicada ⇒ disparar não chamado` com par negativo (T-073/T-075). L187 do projeto.

### N-024 — `set_config(..., true)` é local à TRANSAÇÃO, não à função: SECURITY DEFINER que arma GUC de tenant guarda/restaura e recusa contexto divergente
**Contexto**: multitenancy por RLS com GUC (`current_setting('app.tenant_id')`) e funções `SECURITY DEFINER` que precisam operar em nome de um tenant (sync de auth externo, jobs administrativos).
**Problema**: `set_config(chave, valor, true)` dura até o fim da **transação** — não "volta sozinho" quando a função retorna. Uma função que arma o GUC para o tenant alvo e não restaura deixa o **resto da transação do chamador** rodando como outro tenant: escada de leitura/escrita cross-tenant invisível para a suíte de RLS (que testa policies, não funções). Pior se a função aceita o tenant como parâmetro sem conferir o contexto: qualquer chamador vira `set-tenant` arbitrário.
**Regra**: toda função `SECURITY DEFINER` que arma GUC de tenant: (1) **recusa** chamada cujo parâmetro diverge do GUC vigente (`insufficient_privilege`/`42501`) — a exceção é bootstrap com GUC vazio, explícita no código; (2) captura o valor anterior no início e **restaura no fim** (inclusive no caminho de erro); (3) ganha testes próprios dos 3 casos: divergente recusa sem vazar leitura/escrita, legítimo restaura o GUC, bootstrap volta a `''`. Auditoria de RLS inclui `\df+` das SECURITY DEFINER — as policies não protegem do que roda por dentro delas.
**Origem**: iara, rodada 001-auditoria-fase-0 (12/08/2026) — achado ALTA F0-06-c (`fn_sync_account_from_auth`, `0013`); sonda da Security reproduzida na revalidação; lição L002 do projeto.

### N-025 — Teste de isolamento cross-tenant sem linha semeada é asserção vacuamente verdadeira
**Contexto**: suíte de RLS que varre o catálogo (`pg_catalog`) por tabelas com coluna de tenant e assere "tenant B lê 0 linhas do tenant A" em cada uma.
**Problema**: se a tabela está **vazia**, `count=0` passa sem provar nada — a policy pode nem existir de verdade (ou estar errada) e o teste fica verde. Em rodada real, 73 de 81 tabelas do sweep não recebiam linha semeada: a "cobertura total" comportamental era 10%. O sweep estrutural (exigir `relrowsecurity AND relforcerowsecurity` + ≥1 policy no catálogo) segura a classe, mas não pega policy com predicado errado.
**Regra**: separar as duas garantias e saber qual se tem: (1) **estrutural** — sweep de catálogo, pega tabela sem policy (barato, cobre tudo, entra no CI); (2) **comportamental** — só vale para tabela com linha real do tenant A semeada antes do `count=0` sob B; manter um gerador de linha mínima por tabela (FKs satisfeitas) ou, no mínimo, asserir no próprio teste quantas tabelas foram exercitadas de verdade e tratar a lista das não-semeadas como débito visível, nunca como cobertura.
**Origem**: iara, rodada 001-auditoria-fase-0 (12/08/2026) — achado A-3/N-5 (QA), aberto como débito; `packages/db/test/pg/rls.test.ts`.
**Reforço (12/08/2026, iara, 002-fundacoes-plataforma)**: a classe vale também para **passo de runbook**, não só teste — o passo "prova de isolamento" do runbook de deploy mandava ler `count(*)` da conta B e concluir de `0`, que um banco recém-migrado devolve **com RLS ligado ou desligado** (BL-7, gate de go-live). A prova exige dado dos DOIS lados: semear 2 contas + 1 linha em cada como master, ler como `app_user` com o GUC da conta A e exigir **1** da A e **0** da B — com o doc dizendo que `0/0` = não semeou e `1/1` = RLS desligado. A diferença cabe em duas linhas de runbook; verificação que passa com o sistema desligado é pior que verificação ausente.

### N-026 — Variável obrigatória sem default fora do `.env.example` só quebra em clone limpo: teste de paridade schema×template fecha a classe
**Contexto**: backend Node com schema de env validado (zod/envalid) e um `.env.example` como template de setup.
**Problema**: quem adiciona a variável tem o valor no `.env` local — tudo funciona para o autor, para o CI (secrets) e para quem já clonou. O primeiro clone limpo depois disso morre no fail-fast do schema **sem instrução de recuperação**, e a classe reincide a cada variável nova porque nenhum gate cobre o template (que não é código).
**Regra**: teste de unidade que parseia o `.env.example` e o confronta com o schema: toda chave **obrigatória sem default** do schema precisa existir no template (e, opcionalmente, chave do template desconhecida do schema falha também — pega template apodrecido). Casos do teste incluem o cenário real que motivou (a variável que faltou). Corolário de processo: o valor de exemplo de segredo é explicitamente descartável (`dev-only-…`) para ninguém promovê-lo a staging.
**Origem**: iara, rodada 001-auditoria-fase-0 (12/08/2026) — achado MÉDIA F0-01-d (`AUTH_SECRET`); `packages/config/test/env-example-parity.test.ts`.

### N-027 — Segredo opcional com `''` passa no schema e vira fail-open: colapsar `''→undefined` na fonte única e provar a recusa por comportamento
**Contexto**: backend Node com env validado (zod) e guarda criptográfica cuja chave é opcional por ambiente (HMAC de webhook, verify token) — taskdef/CI/compose declaram a chave com valor **vazio** com frequência (placeholder, secret não preenchido).
**Problema**: `.optional()` valida **ausência**, não vacuidade: `''` atravessa o schema e o código de guarda calcula HMAC com chave vazia — que qualquer atacante conhece. O ramo fail-closed ("sem segredo ⇒ recusa") nunca dispara porque o segredo "existe". Irmão do N-026 (lá a var falta no template; aqui ela existe vazia), com consequência pior: não é boot quebrado, é autenticação furada em silêncio.
**Regra**: (1) toda chave de segurança opcional passa por um transform `''→undefined` **no schema** (fonte única — nunca `if (secret === '')` por call-site); (2) a prova é nas **duas pontas**: teste de schema (`''` ≡ ausente, valor real preservado) **e** teste de comportamento com boot real (chave vazia ⇒ recusa explícita — 503/erro tipado — com **zero** processamento); (3) a recusa ganha evento de log próprio (`*.disabled`) e status que diz a verdade — 401 mentiria a causa ("sua assinatura está errada") e poluiria o alarme de autenticação real.
**Origem**: iara, rodada 003-ingestao-whatsapp (14/08/2026) — OPS-009/R-2/K-2; `optionalText()` em `packages/config` + B-065 com boot real; lição L013 do projeto.
**Reforço (19/08/2026, algar, notificacao-cobranca-whatsapp-hsm) — o irmão NestJS/class-validator do mesmo problema**: ao acrescentar env **opcional** a um `EnvConfig` validado com `class-validator` (`validateEnv` com `skipMissingProperties: false`), o par correto é **`@IsOptional() @IsString()`** — `@IsNotEmpty()` numa var opcional **derruba o boot da API inteira** em todo ambiente que ainda não a provisionou (era exatamente o caso: a URL do runtime nasce ausente de propósito). E o furo simétrico: propriedade **não declarada** na classe atravessa sem validação nenhuma (`plainToInstance` copia o que não tem decorator), então o teste honesto assere que a propriedade **existe declarada** (por `design:type`/metadata) e que o boot **sem** as vars não lança — "tipo errado ⇒ erro" não é asserção válida aqui, porque `enableImplicitConversion` coage número para string.

### N-028 — JetStream: `backoff` na config do consumidor SOBRESCREVE `ack_wait` (server 2.11), e `update` de consumidor não remove campo — backoff vai no `nak(delay)`, config segue expand/contract
**Contexto**: consumer pull durável com `ack_wait` dimensionado pelo pior caso da transação (ex.: 30 s) e retry escalonado desejado (`backoff: [1s,5s,15s,30s]`).
**Problema**: dois modos de falha **medidos** contra `nats:2.11` — (a) com `backoff` declarado na config, o servidor usa `backoff[0]` como janela efetiva de redelivery: os 30 s de `ack_wait` viraram **1 s** e a transação do banco era reentregue **no meio** (retrabalho e corrida sob carga, sem nenhum erro); (b) `update` de consumidor existente **não converge campo removido** da config — o `backoff` antigo sobreviveu ao update, e a config em produção diverge da declarada em código com aparência de sucesso.
**Regra**: (1) não declarar `backoff` na config quando `ack_wait` importa — o escalonamento é decisão do consumidor, aplicada em `nak(delayMs)` (testável, sem efeito colateral no servidor); (2) após provisionar, conferir a config **efetiva** no servidor (consumer info), nunca confiar no que se enviou; (3) mudança de config de consumidor segue **expand/contract como migration**: campo imutável/removido ⇒ consumidor novo versionado, nunca "update e torcer" (recriar só é aceitável em dev).
**Origem**: iara, rodada 003-ingestao-whatsapp (14/08/2026) — IMPL do Dev Backend (frente packages), medido contra broker real (`ack_wait_s=30.0, backoff=None`).

### N-029 — `jsonb` do Postgres recusa NUL em valor E em CHAVE (22P05) e surrogate solto (22P02) — e o remendo `replaceAll` do escape fabrica o segundo veneno
**Contexto**: persistir payload externo hostil (webhook, mensagem de usuário) em coluna `jsonb`; a entrada inclui coisas **digitáveis por qualquer pessoa** (o texto literal `\u0000` — os 6 caracteres digitados, não o code point — e emoji).
**Problema**: três venenos da mesma classe, medidos contra o banco real — valor com U+0000 ⇒ `22P05`; **chave** com U+0000 ⇒ `22P05` (saneamento por campo não alcança chaves); escape de surrogate solto ⇒ `22P02`. E o remendo ingênuo `replaceAll('\\u0000','')` sobre o JSON serializado **come a barra** de quem digitou o texto literal e produz JSON inválido — troca um veneno disparável por outro. Duas armadilhas satélites: truncar texto por `slice` de code units **parte par surrogate bem formado no limite** (emoji legítimo vira veneno novo); e o ORM pode embrulhar o SQLSTATE no `cause` (drizzle 0.44) — classificar sem andar a cadeia deixa o tratamento decorativo (`code: unknown`).
**Regra**: (1) saneamento no **serializador** (cobre valor, chave e surrogate solto), removendo escape só quando a corrida de barras é ímpar — nunca `replaceAll` cego; (2) truncar **primeiro**, varrer surrogate **depois**; (3) correção de saneamento se mede contra o Postgres **real** antes de virar recomendação — o teste unitário de string passa com o remendo quebrado; (4) classificação de erro anda a cadeia de `cause` até o SQLSTATE: dado inválido (classe 22/23) não se re-tenta (vira log+aceite consciente), erro de conexão propaga (o retry do produtor é a única chance da mensagem).
**Origem**: iara, rodada 003-ingestao-whatsapp (14/08/2026) — Security F-1/F-3; sonda medida no FIX do Dev Backend (`paraJsonb`/`sanearTexto`); provado por mutação (M-A/M-B do EVALUATION).

### N-030 — `pg_advisory_lock` de sessão × pool do Prisma: adquire numa conexão, "libera" em outra e fica retido — para efeito externo longo, freio de processo com limitação declarada E verificada
**Contexto**: operação administrativa que serializa um lote de chamadas HTTP externas com custo (ex.: backfill de ids consultando um provider 1× por linha, teto de centenas de chamadas) e precisa recusar execução concorrente ("nunca em paralelo").
**Problema**: a tentação é `pg_advisory_lock(chave)` (lock de **sessão**), mas o Prisma despacha cada query numa conexão qualquer do pool: o `SELECT pg_advisory_lock` sai numa conexão e o `pg_advisory_unlock` pode sair em **outra** ⇒ o lock nunca é liberado (fica retido até a conexão morrer) e a 2ª chamada bloqueia/recusa para sempre. A variante correta para pool — `pg_advisory_xact_lock` — exige manter uma `$transaction` interativa **aberta durante todas as chamadas HTTP** (viola N-008, segura 1 conexão por minutos, estoura o timeout da transação); ela serve para seções **curtas e só-banco** (é como o mesmo projeto usa em `gerarFaturasCiclo`), não para lote com rede.
**Regra**: (1) lock de sessão do Postgres só com conexão dedicada e explícita (fora do pool) — com Prisma padrão, **não usar**; (2) para lote com rede: **freio de processo** (flag `emExecucao` na instância do serviço, checada **antes de qualquer leitura**, `409` para a 2ª chamada, liberada em `finally`) + **cooldown em memória** por alvo com resultado determinístico (ex.: 404 esfria por N h; 429/timeout não esfriam — são transitórios e a operação quer reprocessá-los), com expurgo no início de cada execução; (3) a limitação "protege 1 processo" é **declarada no código e verificada no artefato de deploy** (PM2 fork mode: `pm2 start` sem `-i`/`--instances`/`--exec-mode` — o avaliador abre o script, não aceita o comentário; e registra que restarts, inclusive o automático por `--max-memory-restart`, zeram o cooldown); (4) virar rotina/multi-instância exige migrar o freio para o **banco** (`SELECT … FOR UPDATE SKIP LOCKED` numa linha de "execução em curso" ou tabela de cooldown) — anotar como débito com gatilho objetivo; (5) prova por mutação: sem freio ⇒ 2 execuções simultâneas chamam o provider 2×; sem cooldown ⇒ 3 invocações = 3 chamadas pagas (com = 1).
**Origem**: algar, rodada notificacao-sms-algar (17/08/2026) — SEC-F1/OPS-F06 do backfill de `id_linha_algar`; recusa do advisory lock avaliada e aprovada pelo QA (`scripts/deploy/application-start.sh:13-19` conferido); achado N-1 (`--max-memory-restart 800M`).

### N-031 — `db.execute(sql\`…\`)` do drizzle devolve `timestamptz` como TEXTO, não `Date` — e `texto <= Date` em JS é `false` em silêncio
**Contexto**: worker/serviço que lê timestamps por SQL cru pelo drizzle (`db.execute(sql\`SELECT session_expires_at …\`)`) e compara com o relógio em JS (janela de 24 h do WhatsApp, lease, `due_at`, cooldown).
**Problema**: no caminho `execute` do drizzle (0.44, driver `pg`) o `timestamptz` chega como **string** (`'2026-08-18 03:12:44.123+00'`), não como `Date` — diferente do query builder tipado. `'2026-…' <= new Date()` não lança: JS converte o `Date` para número e a string para `NaN`, e a comparação é `false` sempre. Na 004 a janela de 24 h **simplesmente não existia** (toda mensagem parecia dentro da janela) e nenhum teste unitário pegava, porque o fake devolvia `Date`. Achado do Dev, confirmado pelo QA contra o Postgres real.
**Regra**: (1) todo timestamp lido por SQL cru passa por **um** conversor na borda (`instante(v): Date` que aceita `string | Date`) antes de qualquer comparação — nunca comparar o valor cru; (2) o carimbo que **volta** ao banco vai como veio (é chave de partição/dedupe — L011: nunca reformatar); (3) o teste da janela roda contra o banco real (Testcontainers) com a linha semeada **fora** da janela e assere a recusa — fake que devolve `Date` é falso-verde por construção; (4) alternativa: query builder tipado (`db.select()`), que devolve `Date` — mas misturar os dois caminhos no mesmo módulo é o que esconde o bug.
**Origem**: iara, rodada 004-outbound-whatsapp (18/08/2026) — Dev Backend 2b (`drafts/AVISOS.step5.md`, "armadilha nova"), confirmado no EVALUATION §Aprendizados; `apps/worker-outbound/src/send.processor.ts` (`instante()`).

### N-032 — `tsx` resolve o `tsconfig` pelo CWD: script/app com `paths` próprios só sobe pelo script do PRÓPRIO pacote
**Contexto**: monorepo pnpm com ESM puro sem build (Node ≥24 + `tsx`), cada pacote com `tsconfig.json` que estende `tooling/tsconfig` e pode declarar `paths`/`compilerOptions` próprios.
**Problema**: `tsx` lê o `tsconfig.json` do **diretório atual**, não do arquivo executado. Rodar `pnpm exec tsx apps/api/src/main.ts` (ou um utilitário de `packages/db`) **da raiz** usa o tsconfig da raiz (ou nenhum): `paths` não resolvem, `import` com alias falha com `Cannot find module`, e o erro parece ser do arquivo. Irmão do J-004 (`npx jest` avulso ignora a config do workspace).
**Regra**: (1) app/worker/script sobe **sempre** pelo script do próprio `package.json` (`pnpm --filter @iara/api dev`, `pnpm --filter @iara/db migrate`) — que roda com cwd no pacote; (2) utilitário compartilhado (seed, migrate, sonda) importa por **caminho relativo/absoluto**, nunca por alias de `paths`, para poder ser chamado de qualquer cwd (Dockerfile, task one-off, CI); (3) runbook e CI usam os scripts do pacote, não `tsx <arquivo>` cru; erro de resolução que só aparece numa forma de invocação é do **cwd**, não do código.
**Origem**: iara, rodada 004-outbound-whatsapp (18/08/2026) — EVALUATION §Aprendizados (utilitário rodado da raiz sobre pacote com `paths`).

### N-033 — `pnpm install --frozen-lockfile` reprova importer VAZIO: dep adicionada com `--offline` (ou à mão) deixa o lockfile parcialmente atualizado
**Contexto**: monorepo pnpm; app novo (`apps/worker-outbound`) que ganha dependências durante a rodada (`@sentry/node`, `@opentelemetry/*`), instaladas com `pnpm install --offline` porque a rede/registry estava indisponível.
**Problema**: o `--offline` resolveu do store e "funcionou" localmente, mas o `pnpm-lock.yaml` ficou com o **importer do pacote vazio** (`apps/worker-outbound: {}`) — o CI, que roda `pnpm install --frozen-lockfile`, teria reprovado com `ERR_PNPM_OUTDATED_LOCKFILE` no primeiro push. Passou despercebido por toda a implementação e só apareceu quando a avaliação rodou o comando do CI na it. 2. Modo de falha silencioso: `pnpm test`/`typecheck` locais passam com o lockfile inconsistente.
**Regra**: (1) toda mudança de dependência é seguida de `pnpm install --frozen-lockfile` **local** (é barato e é o comando do CI) — entra na lista de gates do dev no Passo 5 e do avaliador no Passo 6; (2) `--offline` é para rodar, não para **declarar** dependência: depois que a rede volta, `pnpm install` normal e diff do lockfile no PR; (3) o avaliador do Passo 6 confere o importer do pacote novo no `pnpm-lock.yaml` (não pode ser `{}` se o `package.json` tem deps); (4) o `deploy.yml`/Dockerfile que usa `turbo prune --docker` herda o mesmo lockfile — importer vazio derruba a imagem no `pnpm install` do build, longe do dev.
**Origem**: iara, rodada 004-outbound-whatsapp (18/08/2026) — Dev 2b (`pnpm install --offline`), aviso do DevOps na onda 1, detectado e corrigido na it. 2 do EVALUATION (`Already up to date`).

### N-034 — `Nats-Msg-Id` de evento DERIVADO (status de mensagem) é composto `<id-base>:<discriminador>` — id puro faz o JetStream engolir o 2º e o 3º evento como duplicata
**Contexto**: JetStream com `duplicate_window` (dedupe L1 por `Nats-Msg-Id`) e um provedor que emite **vários** eventos sobre a mesma entidade externa — WhatsApp: um `wamid` gera `sent`, `delivered`, `read` (e `failed`); Stripe: um objeto gera N eventos.
**Problema**: reusar o `Nats-Msg-Id` da entidade (`meta:<wamid>`) para o status faz o broker tratar `delivered` e `read` como duplicatas do `sent` dentro da janela — a timeline **para no primeiro status sem erro em lugar nenhum** (publish 200, ack ok, nada chega ao consumidor). É invisível em teste unitário e só aparece contra o broker real ou em produção.
**Regra**: (1) evento derivado carrega o discriminador no id: `` `${metaMsgId(wamid)}:${status}` `` (a função de id da entidade + `:` + o discriminador, este passando pelo próprio charset fechado — CR/LF em header é injeção); (2) o dedupe L2 no consumidor usa a mesma chave `(entidade, discriminador)` (`UNIQUE (message_id, status, created_at)` com `created_at` determinístico — L011/N-029 do mesmo projeto); (3) o **subject** pode e deve continuar o da entidade (mesma conversa) — id e subject têm papéis diferentes: um deduplica, o outro roteia/ordena; (4) teste com broker real: publicar `sent` e `delivered` do mesmo wamid dentro da janela e asserir **2** entregas.
**Origem**: iara, rodada 004-outbound-whatsapp (18/08/2026) — D-125 do CONTRACT (`statusMsgId` em `packages/events/src/subjects.ts`), B-088/B-089, `webhook-status.test.ts`.

### N-035 — Adapter que promete "nunca lançar": o `try` começa no PRIMEIRO degrau com I/O — e log de sucesso dentro do `try` transforma efeito ENTREGUE em erro na trilha

**Contexto**: adapter/canal (notificação, provider de pagamento, gateway) chamado por um motor de lote (job/cron) que persiste o desfecho numa trilha e **não pode parar** por falha de um item. O contrato é "resolve sempre" (`ENVIADA`/`ERRO`), nunca lança.
**Problema**: três furos que convivem com a suíte verde. (1) O `try` começava **depois** da consulta ao repositório (resolver o template/HSM): erro do Prisma (`P2021`/`P2022` com migration pendente, queda de conexão, `42501`) escapa como **exceção**; o motor a captura no genérico e persiste `err.message` **cru** numa coluna servida por API — trilha mentirosa (o canal aparece como falha de envio) **e** vazamento de detalhe interno. (2) O log de **sucesso** vivia dentro do `try` do envio: um transporte de log quebrado transforma mensagem **já entregue** — irreversível e cobrada — em `ERRO` na trilha. (3) A observabilidade (anúncio de configuração, hash da URL) rodava **antes** do `try`, ou seja, o próprio código de diagnóstico podia derrubar o envio.
**Regra**: (1) o `try` do adapter abre no **primeiro degrau que toca I/O**, repositório incluído; o que roda antes dele é puro e null-guarded (`optional chaining`, `String(v ?? '').trim()`, `new URL` embrulhado em try/catch próprio); (2) **todo** log do adapter sai por um helper (`logar()`) que engole falha do transporte — `this.logger.` direto na classe deve ter **0 ocorrências**, e isso é verificável por spec estrutural; (3) no `catch`, o corpo do erro é **sempre string** antes de qualquer `JSON.stringify` (evita o caso circular) e passa pela **redação** de segredo/PII; (4) a bateria prova com ≥3 códigos de erro do ORM ⇒ o método **resolve** com status de erro **e** `expect(fetch).not.toHaveBeenCalled()`, mais um caso "repositório sem o método" (contrato quebrado em runtime).
**Origem**: algar, rodada notificacao-cobranca-whatsapp-hsm (19/08/2026) — S-02/S-02d do Security (SEC-926), reverificados por auditoria statement a statement e por mutação.

### N-036 — `@Optional()` não satisfeito vira `undefined` EM SILÊNCIO: escopo de módulo do Nest não é transitivo, e teste que instancia o service com `new` não prova fiação

**Contexto**: service de um módulo (ex.: `PosPagamentoService`) que recebe uma dependência de outro módulo como `@Optional() dep?: XService` — padrão legítimo para acoplamento fraco/feature aditiva. Os specs unitários instanciam o service à mão (`new Service(a, b, ...)`) com dublês.
**Problema**: se o módulo consumidor **não importa** o módulo provedor (`imports: [XModule]`), o container resolve o `@Optional()` para `undefined` **sem erro, sem warning, sem log** — e o comportamento resultante ("não faz nada") é **indistinguível de sucesso** em qualquer teste de unidade, porque `new Service(...)` contorna o DI por completo. Numa rodada real, a feature inteira subiu **desligada** (a ativação pós-pagamento nunca rodava, com 2 invariantes financeiros abertos em produção) e **441/441 testes passavam** — os specs provavam a lógica dos gates com a dependência injetada à mão. O escopo de módulo do Nest **não é transitivo**: registrar o provider no `providers` de outro módulo, ou tê-lo exportado em algum lugar da árvore, não o entrega a quem não o importa.
**Regra**: (1) toda dependência `@Optional()` nova ganha **1 teste de fiação** que compila o módulo real (`Test.createTestingModule({ imports: [ModuloConsumidor] })`, bordas de I/O por `overrideProvider`) e assere `service.dep !== undefined` — sem isso o `@Optional()` é um interruptor desligado que ninguém vê; (2) quando o `compile()` está bloqueado por infra (ex.: dependência ESM-only — `@scure/base` via `otplib` — que o transform do Jest não parseia), o substituto aceitável é o spec **estrutural** (`fs` + regex sobre `imports`/`exports` **com comentários removidos** — senão um comentário citando a classe satisfaz o teste), com a causa verificada por terceiro e a limitação **declarada** (não pega ciclo de módulo nem `forwardRef` mal resolvido) — e o `transformIgnorePatterns` vira dívida de infra do projeto; (3) sinal de alarme em avaliação: `@Optional()` + specs com `new ... as any` + suíte 100% verde na 1ª execução do Passo 6 = **suspeitar de fiação antes de comemorar**; (4) irmã de campo: função implementada e nunca chamada é indistinguível de ausente (P-032) — a guarda de fiação fecha as duas classes.
**Origem**: algar, rodada pacote-adicional-dados (20/08/2026) — bloqueante C-01 (`PosPagamentoModule` sem `ClausulaPacoteModule` nos imports; RN-004 morta com suíte verde), fechado na it.2 e provado pela mutação MA (remover o import ⇒ 2 vermelhos); limitação ESM registrada no achado C-39.
**Reforço (21/08/2026, algar, portal-contrato-cliente)**: reincidiu **duas vezes na mesma rodada** — o gancho de provisionamento no `finalizar` (`@Optional() portalCredencial?`) e o limiter do relatório — ambos "funcionando" em unit e **inertes** na fiação real (Q-B/B-057). Par mínimo que passou a ser exigido no CONTRACT: `@Inject(TOKEN)` explícito (não só o tipo), ramo `else` que emite `warn resultado=dependencia-ausente` (a ausência deixa rastro em produção) e teste do **call-site** que assere o efeito (não o `!== undefined` só). Sinal operacional correspondente entrou no runbook (`grep resultado=dependencia-ausente` nas 48 h).

**Reforço (25/08/2026, algar, skeelo-ativacao-clientes)**: a variante **fatal** da mesma classe — `@Optional() x?: Interface` **sem `@Inject`** num repositório novo não vira `undefined` silencioso: o token é `undefined` no `design:paramtypes` e o container derruba o bootstrap inteiro (`UnknownDependenciesException … at index [1]`), com `nest build` e 340 + 10 000 testes verdes. O par mínimo `@Optional() @Inject(TOKEN)` da regra (21/08) passou a ser **obrigatório** e a guarda de fiação compila o módulo real e assere **por parâmetro** (mutante: remover o `@Inject`) — ver N-047 (boot do `dist` como aceite, Prisma preguiçoso ⇒ sem banco, `perl alarm`).

**Reforço (27/08/2026, algar, integracao-d4sign) — 4ª reincidência, agora com boot do `dist` VERDE**: `D4SignModule` ficou fora dos `imports` de `PedidoModule`/`ClausulaContratoModule`/`PortalContratoModule`; `nest build`, 770/770 da feature, suíte completa em delta zero e o **boot do `dist`** (`[InstanceLoader] D4SignModule dependencies initialized`, `BOOT_DI_OK`) passaram — e a guarda que bloqueia a ativação de linha PJ sem contrato assinado **nunca rodava** no app montado (com o gate `'S'`, a linha ativava na operadora sem nada acusar). Precisão sobre a regra (1): o boot prova **ausência de erro de DI**, não **presença de injeção** — `dependencies initialized` fala do módulo, não do hospedeiro. A guarda que fechou: (a) spec que compila **cada hospedeiro real** (`Test.createTestingModule({ imports:[PedidoModule] })`, I/O por `overrideProvider`) e resolve o token do gancho por `get()`; (b) **controle negativo executado** (comentar `D4SignModule,` nos 3 ⇒ 7/9 casos vermelhos — sem isso o spec pode ser vácuo); (c) o `d4sign.module.di.spec.ts` que compilava o módulo **isolado** e o comentário no `d4sign.module.ts` que **descrevia literalmente o defeito** não valem como guarda. Origem do furo: a feature copiou o **código** do molde (`skeelo-ganchos.spec.ts:500-507` já assertava a fiação nos hospedeiros) mas não o **spec** — molde copiado traz a lista de specs junto. Forma de carga do spec de fiação: ver N-051.

**Reforço (27/08/2026, algar, ciclo-vida-contratual) — 5ª reincidência; a regra ENDURECE**: o serviço hospedeiro (`ContratoDocumentoService`, tocado pela feature) injetava o disparo de e-mail com `@Optional()` + `if (!this.disparo) return`, e o controller tinha `@Optional() renovacaoService` (⇒ **400** em runtime se a fiação quebrasse). A fiação estava **certa** — o módulo já importava o provedor — o que torna o `@Optional()` **só desvantagem**: transforma quebra futura em feature verde que nunca envia. Regra endurecida: (5) **injeção obrigatória é o default**; `@Optional()` só quando o hospedeiro **não** importa o provedor **por desenho** (feature aditiva atrás de gate), e então com justificativa escrita no CONTRACT + `@Inject(TOKEN)` + `else` com `warn` (reforço 21/08); se o módulo importa o provedor, `@Optional()` é **proibido** e a correção é removê-lo (falha de fiação vira boot vermelho); (6) o spec de fiação cobre **todo hospedeiro tocado pelo diff**, não só o módulo da feature, com controle negativo executado **e assertando o motivo** (a mensagem cita o token esperado); (7) guarda de fonte "sem `@Optional()`" remove comentários **e** carrega um trecho sintético que o detector PRECISA acusar — aqui deu falso positivo no arquivo cujo JSDoc explicava *por que* não tem `@Optional()` (QA-2). Achado por DevOps e Security **independentemente** (P-048). L174 do projeto (reforço).

### N-037 — Flag/knob de banco com "ausente = DESLIGADO": a semântica do default é COMPORTAMENTO e ganha teste no leitor REAL — gate sempre mockado = semântica nunca fixada

**Contexto**: feature atrás de flag operacional em tabela de parâmetros (`parametro_sistema` e afins), com a decisão de rollout "ausente/`'N'`/vazio = OFF; só `'S'` liga" — o estado de go-live é justamente o da flag ausente.
**Problema**: todos os testes do gate mockavam o leitor (`gateAtivo()` dublado devolvendo `true`/`false`), então **nenhum teste pinava a semântica**: a mutação `?? ''` → `?? 'S'` (ausente = **LIGADO** — a inversão exata que outra feature do mesmo projeto levou a produção) passou com **510/510 verdes**. A leitura do código aprovaria: a implementação estava certa; o que faltava era a guarda contra o refactor que a inverte. Agravante comum: `findFirst` sem `ORDER BY` na chave — duplicata viva da linha = comportamento indeterminístico entre requisições.
**Regra**: (1) o **repositório/leitor real** da flag ganha spec próprio com a tabela de valores hostis: `null` (linha ausente), `''`, `'N'`, lixo (`'SIM'`, `'s'`, `' S '`) ⇒ postura documentada; `'S'` ⇒ ligado — decidindo **na fixture do Prisma/driver mockado no nível de linha**, nunca mockando o método do gate; (2) a prova é **mutação da direção do default** (`?? ''` → `?? 'S'` tem de deixar a suíte vermelha) — é o mutante mais barato e mais valioso de qualquer feature com flag; (3) a semântica real implementada (case-insensitive? trim?) fica **documentada no runbook** se divergir do contrato, e o teste a fixa como está; (4) leitor com `findFirst` sem `ORDER BY` ⇒ a conferência de go-live conta linhas vivas da chave (`= 1`).
**Origem**: algar, rodada pacote-adicional-dados (20/08/2026) — achado C-12 do DevOps (mutante MK sobreviveu na it.2 com 510/510; fechado na it.3 — MK passa a matar 2 testes); precedente: `catalogo-resolucao-por-codigo-e-frase` (knob com ausente = LIGADO em produção).

### N-038 — Env lida por dotenv/`ConfigModule` NÃO está em `/proc/<pid>/environ`: a prova de que o `pm2 restart` "pegou" é o anúncio de boot (`onModuleInit` + `sha8`) e o hash do arquivo — e `sha256sum` de pipe hasheia o newline

**Contexto**: NestJS sob PM2 (fork) com `.env` carregado em runtime (`@nestjs/config`/dotenv), env nova de segredo (URL/credencial) cujo rollout e rollback são "editar env + `pm2 restart`"; o runbook precisa provar que o processo está com o valor certo **sem imprimir o valor**.
**Problema**: comandos herdados de runbook anterior pareciam válidos e eram inertes ou enganosos: (a) `cat /proc/<pid>/environ | tr '\0' '\n' | grep CHAVE | sha256sum` devolve **sempre** `e3b0c442…` (hash da string vazia) porque o environ de exec só tem o que o script de start exportou — env lida por dotenv **não aparece** lá; "não bateu ⇒ restart de novo" vira loop com a env já correta; (b) `echo/sed … | sha256sum` inclui o **`\n`** e dá um `sha8` diferente do que o código calcula sobre a string (`0bf10640` × `d9e5e994`) ⇒ operador conclui "URL errada" com URL certa; (c) `pgrep -f 'dist/main'` não casa quando o start roda `dist/src/main.js`.
**Regra**: (1) o serviço que consome a env anuncia no **`onModuleInit`** (não preguiçoso no 1º uso — senão a prova depende de haver tráfego) uma linha estável por item: `[marca] destino=<x> configurado=<bool> sha8=<sha256(valor)[0:8]|-> causa=<slug|->`, e o runbook prova o restart por **grep dessa linha** + `printf '%s' "<valor>" | shasum -a 256 | cut -c1-8` (sem newline; valor gravado **sem aspas** no arquivo de env); (2) `grep -c CHAVE /proc/<pid>/environ ⇒ 0` é o **esperado** para env de dotenv — o runbook diz isso explicitamente para ninguém "corrigir"; (3) todo comando de prova herdado declara o **mecanismo** (environ de exec × dotenv × log de boot × arquivo) e é **rodado** contra o `dist/` compilado antes de entrar no runbook (P-037) — se o mecanismo mudou, o comando morre junto; (4) env lida **por chamada** (`process.env.X` dentro do método) serve à testabilidade, **não** promete troca a quente sob PM2 — o runbook não pode sugerir "edite e espere".
**Origem**: algar, rodada bot-webhook-dual-destino (20/08/2026) — 3 comandos copiados do runbook do canal HSM (§27.4/§27.7) corrigidos pelo DevOps no Passo 5 após renderizar as linhas reais (8/8 greps casaram); lição de projeto L131.

### N-039 — `ValidationPipe` global pré-empta o pipe local de `@Body`: envelope de erro próprio só existe com o pipe CHAMADO NO HANDLER sobre o corpo bruto (ou `expectedType` + parâmetro não tipado) — e só se prova por teste NO FIO

**Contexto**: app com `useGlobalPipes(new ValidationPipe({ whitelist: true }))` e uma rota nova que precisa de contrato de erro próprio (`{code, message}` string, chave desconhecida ⇒ 400 com precedência sobre constraint de valor), implementada com `@Body(new ValidationPipe({ whitelist, forbidNonWhitelisted, exceptionFactory }))` e um DTO tipado no parâmetro.
**Problema**: o Nest executa `pipes.concat(paramPipes)` (`router-execution-context.js:151`) — o **global roda antes** do local sobre qualquer parâmetro cujo metatype seja uma classe. Resultado medido: o global já descartou a chave desconhecida (`whitelist`) e já lançou o 400 de constraint com o **envelope padrão** (`message: string[]`, sem `code`, mensagem duplicada) antes de o local existir — `forbidNonWhitelisted` e `exceptionFactory` do decorator são **código morto servindo HTTP**. A suíte ficava verde porque o spec instanciava o pipe **isolado** (`new ValidationPipe(...).transform(...)`), que de fato funciona sozinho. Dois efeitos colaterais do mesmo mecanismo: `stripProtoKeys` (apaga `__proto__`/`prototype`/`constructor` antes do `plainToInstance`) **só roda se o pipe validar aquele parâmetro** — com parâmetro tipado, 400 para `__proto__` é insatisfazível; sem ele, a chave chega à guarda e o 400 acontece.
**Regra**: (1) handler com envelope próprio recebe **só o corpo bruto** (`@Body() bruto: Record<string, unknown>` — o global passa direto com metatype `Object`), aplica a guarda de superfície (lista de chaves proibidas, precedência), e só então chama o pipe local **instanciado em módulo** (`pipe.transform(bruto, { metatype: Dto, type: 'body' })` ou `expectedType: Dto`); (2) ≥1 teste **no fio** (`Test.createTestingModule` + `useGlobalPipes` + filtro iguais ao `main.ts`) por rota com envelope próprio — o teste do pipe isolado não prova nada sobre a requisição; o caso pina a **causa** (`Reflect.getMetadata('design:paramtypes', proto, handler)` não contém o DTO), não só o sintoma; (3) `__proto__`: asserir o **efeito** (protótipo intacto + 0 escrita) e tolerar o status como consequência do arranjo (`[200,204,400]`) — foi o que deixou a correção passar sem reescrever o caso; (4) a guarda do corpo bruto é **defesa em profundidade** e não se remove quando o pipe local "passa a funcionar": ela é a única fonte da recusa de `__proto__`, da precedência e do `warn` de diagnóstico (o mutante que a remove cai de 23 para 2 mortes — o que resta é a lista exata do que só ela faz); (5) o CONTRACT descreve **envelope + precedência + mensagens**, nunca o mecanismo (`@Body(new ValidationPipe)`) — um contrato que prescreve o meio pode prescrever um meio inviável.
**Origem**: algar, rodada shell-linha-acoes-e-assinatura (20/08/2026) — E-01 (bloqueante da it.1, convergência QA+Security+DevOps), corrigido na it.2 (M-15b morre com 36 ❌); lição de projeto L133. Estende N-009 (global sem `transform` exige local): não basta o local, o global **não pode ver** o DTO.

### N-040 — Spec de varredura RBAC por `prototype` obriga helper de controller a ser função de MÓDULO — e o verbo vem do enum `RequestMethod`, nunca de mapa à mão

**Contexto**: guarda sistêmica de authz (ver P-127-like: `PermissionsGuard` fail-open por omissão) implementada como spec que percorre `Object.getOwnPropertyNames(Controller.prototype)` exigindo `@RequirePermission` em todo método; controller que ganha rotas novas e, com elas, pequenos helpers (`recusa()`, `montarDto()`).
**Problema**: um helper `private` na classe é um nome no prototype ⇒ a varredura o trata como **handler sem permissão** e a guarda de segurança fica **vermelha por ruído** — o pior lugar para falso positivo, porque ensina a ignorá-la ou a relaxá-la com uma allowlist (que depois esconde um handler real). Segundo modo de falha: spec que mapeia o verbo com `{0:'GET',1:'POST'}` à mão não conhece `PATCH` (= **4** em `RequestMethod`) e falha por motivo errado na primeira rota PATCH.
**Regra**: (1) helpers de controller vivem **fora da classe** (funções de módulo no mesmo arquivo, ou util importado) — a classe só tem handlers; (2) a varredura deriva o verbo por reverse lookup de `RequestMethod[...]` e assere **também** os guards na classe (`@UseGuards(JwtGuard, PermissionsGuard)`); (3) rota **removida** se prova como 404 do Nest (sem metadata de rota), nunca como "handler sem decorator".
**Origem**: algar, rodada shell-linha-acoes-e-assinatura (20/08/2026) — D-it2-4 (quase-regressão evitada no loop) e C-11 do CONTRACT; lição de projeto L134.

### N-041 — 403 (e 401) NÃO passam por interceptor: log de auditoria por interceptor mede uso AUTORIZADO, nunca tentativa barrada

**Contexto**: auditoria de acesso implementada como `NestInterceptor` (grava método/rota/usuário/payload em tabela) e runbook/censo que quer saber "quem tentou chamar a rota X" ou "o 403 apareceu no log".
**Problema**: a ordem de execução do Nest é guards → interceptors → pipes → handler; guard que nega (`ForbiddenException`/`UnauthorizedException`) encerra a requisição **antes** do interceptor ⇒ 403/401 nunca chegam à tabela. O runbook pedia evidência inexistente ("conferir o 403 no log") e o censo de uso de uma rota a ser removida ficava sem calibragem — "0 linhas" indistinguível de query errada. O `HttpExceptionFilter` padrão também costuma logar só 5xx ⇒ 4xx de rota de escrita ficam mudos.
**Regra**: (1) documentar no runbook que a tabela de auditoria é de **uso autorizado**; tentativa barrada só é observável por `warn` no guard/filter ou no access log do nginx/ALB; (2) todo censo por tabela de auditoria tem **controle positivo** (uma chamada conhecida que TEM de aparecer) antes de concluir "0 usos"; (3) rota de escrita que precisa de diagnóstico emite o próprio `warn` com slug em **todo 4xx** (ver P-201) — senão "ninguém usa" e "o gate recusa todo mundo" são indistinguíveis.
**Origem**: algar, rodada shell-linha-acoes-e-assinatura (20/08/2026) — erro factual do runbook §31 corrigido pelo próprio DevOps no Passo 6 (passo 10 do smoke passou a esperar 403 em silêncio); lição de projeto L139.

### N-042 — Segunda sessão JWT no MESMO emissor/segredo (portal do cliente × painel interno): o guard antigo recusa o claim discriminador ANTES de qualquer I/O, o guard novo exige `=== true` estrito, e o escopo vem do banco — nunca do token

**Contexto**: app NestJS com `JwtGuard` + RBAC para usuários internos ganha uma **2ª população autenticada** (cliente final, parceiro, dispositivo) que precisa de sessão própria, sem RBAC, com TTL e regras distintas — e o caminho barato é reusar o `TokenProvider`/segredo existente com um claim discriminador (`portal: true`).
**Problema**: com o mesmo segredo, **todo** token válido de uma população é um token válido para a outra até que alguém o recuse explicitamente. Três buracos silenciosos: (1) o `JwtGuard` interno aceita o token do portal se o `sub` casar com um `cod_objeto` de usuário (ids inteiros colidem); (2) o guard novo que testa `if (payload.portal)` (truthy) aceita `"false"`, `1`, `{}`; (3) claims que **mudam a checagem** no guard antigo (ex.: `integrador` pula a sessão única e cross-checa outra coisa; `mfaPending`) entram no emissor novo por cópia de payload e abrem atalho. E se o escopo (quais recursos o titular vê) sair do token, a revogação/transferência de titularidade não alcança sessões vivas.
**Regra**: (1) o guard **antigo** ganha a recusa do claim discriminador como **1ª checagem após o `verify`, antes de qualquer I/O** (`if (payload.portal === true) throw Unauthorized`); (2) o guard **novo** exige igualdade **estrita** (`=== true`) e um conjunto **fechado** de claims (`sub`, discriminador, versão da credencial tipo `pwd_at`) — claims do painel (`login`, `permissions`, `integrador`, `mfaPending`) são proibidos no emissor novo por teste estrutural; (3) escopo e estado da credencial são lidos **do banco a cada request** (1 leitura), e o `pwd_at` do token é comparado ao do banco (troca/reset de senha derruba sessões sem lista de revogação); (4) teste de **rejeição cruzada nos 2 sentidos** (token do portal em rota interna ⇒ 401; token do painel no portal ⇒ 401) e, se houver kill-switch, ele roda **na classe** do controller (antes do limiter e do guard de sessão) para que "desligado" custe 0 I/O; (5) `PermissionsGuard`/RBAC **não** entram no controller novo — sem `@RequirePermission` ele libera (fail-open), e com ele exigiria RBAC da população nova.
**Origem**: algar, rodada portal-contrato-cliente (21/08/2026) — `jwt.guard.ts:62` (+1 linha), `PortalContratoGuard` + `PortalDisponivelGuard`, SEC-1010/SEC-1018; lição de projeto L140.


### N-043 — Enhancer (`@UseGuards`/`@UseInterceptors`) por CLASSE resolve as dependências no módulo do CONTROLLER: exportar o enhancer sem exportar os TOKENS dele quebra só o bootstrap

**Contexto**: módulo de domínio implementa um guard/interceptor, o declara em `providers` e o exporta; o controller que o usa vive em OUTRO módulo (um `ApiModule` que concentra a camada de apresentação) e o referencia por classe: `@UseGuards(MeuGuard)`.

**Problema**: o Nest trata enhancer referenciado por classe como injectable **do módulo do controller** — ele o instancia ali e resolve as dependências **naquele** contexto. Exportar a classe do enhancer não basta: cada token que o construtor injeta (`@Inject(Symbol(...))`, serviços próprios) precisa ser visível a partir do módulo do controller. Faltando um, `nest build` compila, a suíte unit fica **100% verde** (os specs instanciam o enhancer com `new`, contornando o DI) e **só o bootstrap** morre: `UnknownDependenciesException ... at index [N] ... available in the XModule`. É o mesmo modo de falha de serviço de infra não exportado — build e teste são cegos a ele por construção.

**Regra**: (1) todo enhancer exportado sai acompanhado dos **tokens de todas as suas dependências** (exceção: o que vem de módulo global — `Reflector`, provider de token/config); (2) a guarda é um spec **estrutural** que lê o construtor do enhancer e exige cada dependência nos `exports` do módulo — barato e não arrasta Prisma/env como compilar o módulo real; (3) o spec estrutural **remove comentários** do trecho antes de comparar: um `includes` sobre a fonte crua casa com o token citado em prosa e o mutante sobrevive (irmã de J-008); (4) o aceite é **subir a aplicação**, não o build — só o boot exercita o container.

**Origem**: algar, feature `portal-contrato-cliente` (21/08/2026) — `PortalContratoModule` exportava `PortalContratoGuard` sem `Symbol(PortalContratoRepository)`; achado ao subir o ambiente de teste, DEPOIS de o EVALUATION aprovar 4/4 com 441 testes verdes e 2 builds limpos.

**Reforço (25/08/2026, algar, skeelo-ativacao-clientes)**: mesma classe **fora de enhancer** — parâmetro `[1]` de um repositório tipado por interface sem `@Inject` (D-1, único bloqueante da rodada, achado pelo DevOps ao subir o artefato). A regra (4) "aceite é SUBIR a app" virou gate **executado** por dois avaliadores; N-047 fixa o mecanismo (`perl -e 'alarm N; exec @ARGV'`, exit 142 = processo morto; Prisma conecta preguiçosamente ⇒ boot sem banco/túnel) e a guarda que compila o módulo em vez do spec estrutural.

### N-044 — Exception filter global com ALLOWLIST de chaves do corpo de erro: campo novo prometido no contrato de erro (`total_calculado`, `data_fim`) nunca chega ao cliente se só o `throw` mudar — e só teste por HTTP prova o fio

**Problema**: um `HttpExceptionFilter` que normaliza o envelope de erro (boa prática: `code`/`campos`/`detalhes` fixos, nada do `message` cru do Nest vaza) é uma **allowlist fechada**. Quando o CONTRACT do Passo 3 promete um campo novo no 409 (`{code:'VALOR_DIVERGENTE', total_calculado}` para o front mostrar o recálculo; `{code:'FIDELIDADE_VIGENTE', data_fim}`) e o service faz `throw new ConflictException({code, total_calculado})`, o teste do service vê o objeto lançado e passa; o filtro descarta a chave; o front recebe só `code`. A suíte fica verde porque nenhum teste atravessa o filtro — achado só pelo teste de pipeline HTTP (`supertest` contra o app com o filtro registrado), adiado no Passo 4 "porque o controller não existia" (P-055).
**Regra**: (1) campo novo do envelope de erro é mudança em **duas pontas**: `throw` + allowlist do filtro, com **guarda de tipo** (número finito / string não-vazia — nunca "passar tudo"), e teste de que chave extra do chamador **não** vaza (a allowlist continua fechada); (2) todo código de erro contratado no §API ganha 1 caso por **HTTP** (app de teste com o filtro global real), que assere o corpo **byte a byte** — o teste do `throw` é teste do service, não do contrato; (3) o RESEARCH de feature que acrescenta campo a erro lista o filtro global como arquivo a tocar (é um call-site escondido do contrato).
**Origem**: algar, rodada troca-assinatura-linha (21/08/2026) — B-089 reprovado na it.1 (`AVISOS.step5.md §6`), `infra/filters/http-exception.filter.ts` com `total_calculado`/`data_fim` sob guarda de tipo; `troca-assinatura.pipeline.spec.ts`; L147 do projeto.

**Reforço (28/08/2026, algar, cadastro-minutas-documento-aceite — C-11/RN-009d)**: a mesma classe num redator de **persistência/log**, não só no filtro HTTP: `registrarFalha` gravava `dados_falha` por um redator com allowlist `status|error|message` e **apagava** `causa`/`quantidade` que o próprio serviço acabara de pôr — o service testado com dublê estava verde, porque o dublê não redigia. Campo diagnóstico novo entra na allowlist do redator (`CHAVES_DIAGNOSTICO_LOCAL`, aceita **só** pela função rasa; o deep-walk do corpo do parceiro fica inalterado — nenhuma porta nova) **no mesmo diff**, e a prova é round-trip **no repositório** (B-069), não no service.

### N-045 — Migration numerada por slot (`YYYYMMDD_NN_nome`) em árvore compartilhada: spec, runbook e allowlists descobrem a pasta pelo SUFIXO/nome, nunca pelo número — e o dev confere o slot ao criar a pasta

**Contexto**: convenção de migrations Prisma/SQL com prefixo de data + sequência (`20260821_03_minha-feature/migration.sql`), specs que validam a migration (lendo o `.sql`), tripwires de allowlist ("nenhuma migration posterior a X") e runbooks que citam o caminho — em repositório onde **mais de uma rodada** cria migration no mesmo dia.
**Problema**: o CONTRACT fixou `_03`; durante o Passo 4 outra feature em voo criou `20260821_03_…`, a spec passou a `_04`; durante o Passo 5 uma terceira tomou o `_04` e a pasta final foi **`_05`** — o slot andou **duas vezes numa única rodada**, e cada vez quebrava a spec de migration, o runbook (§35.3 ainda citava `_04` no Passo 6) e os tripwires de allowlist das features vizinhas (11 suítes listam nominalmente as migrations esperadas; a pasta alheia derrubou uma delas). O número é a parte **menos estável** do nome e a única que o código não controla.
**Regra**: (1) spec de migration localiza a pasta por **glob do sufixo** (`*_nome-da-feature`) e assere **unicidade de prefixo** (exatamente 1 pasta com esse nome) — nunca hardcoda `_NN`; (2) runbook, allowlists e CONTRACT citam o **nome** (`<data>_NN_nome` com NN anotado como "conferir no deploy") e a ordem de aplicação é por **dependência declarada** (guarda `RAISE` na própria migration — N-012/N-021), não pela sequência numérica; (3) o dev-backend confere o slot livre **no momento de criar a pasta** (`ls prisma/migrations | grep <data>_`) e registra o número final no `AVISOS`/RESUME — o CONTRACT só promete o sufixo; (4) tripwire de allowlist de outra feature que quebra por pasta alheia é **pedágio esperado** (L077 do projeto): evolui com anotação, nunca com edição-reflexo; (5) evidência de não-contaminação entre rodadas é mecânica: varrer os diffs das suítes de migration e contar quantas vezes cada pasta aparece como "inesperada".
**Origem**: algar, rodada nfse-nacional-e-campos-nota (21/08/2026) — CONTRACT D-01 revisado no Passo 4 (escalação do QA), RUN.jsonl notas 70/95/98, EVALUATION §1.1; L152 do projeto. Par operacional de P-032 (reforço 21/08).

**Reforço (28/08/2026, algar, cadastro-minutas-documento-aceite ∥ pedido-finalizacao-automatica)**: o slot `_01` do SPEC foi tomado pela rodada paralela ⇒ `_02`, descoberto pelo sufixo como manda a regra. Efeito novo: a allowlist `POSTERIORES_REVISADAS_DECLARADAS` vive em **11 specs** e as 2 rodadas acrescentaram 1 linha cada ⇒ **hunk compartilhado** que o commit seletivo de uma rodada quebra (a lista cita pasta não commitada) — e 2 desses specs já eram vermelhos por dívida, mascarando o sinal. Ver P-072.

### N-046 — Cron novo confere a VIZINHANÇA do slot: jobs no mesmo minuto que batem no mesmo provider externo (mesmo cliente OAuth) disputam token e rate limit — e `timeZone` explícito é obrigatório

**Contexto**: NestJS `@nestjs/schedule` com vários `@Cron` em `src/infra/jobs/`, parte deles chamando a **mesma** API externa (OAuth2 client credentials com token em cache + rate limit 429 sem `Retry-After`), em app sob PM2 fork/1.
**Problema**: a SPEC fixou o job novo em `0 9 * * *` por ser "horário comercial"; o slot `0 9` **já tinha 3 jobs**, um deles chamando a mesma API com o mesmo cliente OAuth — no mesmo minuto, os dois disputam o refresh do token e somam chamadas no mesmo rate limit: o 429 aparece como "erro do job novo" sendo concorrência de vizinhança. Ninguém olha o mapa de slots porque cada feature só vê o próprio job. Agravante clássico: `@Cron('15 9 * * *')` **sem** `timeZone` roda em UTC — 06:15 BRT, no meio da janela de outros jobs e fora do horário comercial que a regra de negócio exigia.
**Regra**: (1) todo job novo traz no RESEARCH/CONTRACT o `grep -rn "@Cron(" src` com a lista de **integrações externas** que cada vizinho toca e escolhe um minuto **vazio** para o mesmo provider (aqui `15 9`); (2) `{ name, timeZone }` explícitos sempre (sem `timeZone` o horário é UTC; sem `name` não há como desligar/diagnosticar); guard de reentrada + `finally` + `catch` que só loga; (3) a janela proibida de deploy do runbook é **derivada** do mapa de slots — job novo dentro dela não a amplia, job fora dela precisa dizer que a amplia; (4) o mapa de slots vira seção do runbook/LESSONS do projeto e é reconferido a cada job novo (ele envelhece: aqui 21 crons, 1 legado sem `timeZone`). Par de P-012 (feature que gasta dinheiro) quando o provider cobra por chamada.
**Origem**: algar, rodada notificacao-consumo-dados-sms (22/08/2026) — CONTRACT C-07 (DevOps D-O1), `DEPLOY.md §36` janela 06:00–10:15; L159 do projeto (mapa de slots).

### N-047 — Parâmetro de construtor tipado por INTERFACE sem `@Inject(TOKEN)` compila, passa 100% da unit e mata o BOOT: guarda de fiação que compila o módulo + boot do `dist` como gate de deployability (Prisma conecta preguiçosamente ⇒ prova-se sem banco)

**Contexto**: repositório/serviço novo num módulo próprio (`XModule`) recebendo um colaborador por interface de domínio (`@Optional() parametros?: ParametroSistemaRepositoryInterface`) — sem `@Inject(TOKEN)`, confiando no `?` do TypeScript; bateria unitária que instancia tudo com `new`; `nest build` no CI; boot só em produção via `pm2 restart`.
**Problema**: interface não existe em runtime ⇒ `design:paramtypes[i] === undefined` ⇒ o container lança `UnknownDependenciesException … at index [i]` no bootstrap — e **só** ali: `nest build` checa tipos, e as 340 asserções da feature (mais 10 000 da suíte) instanciavam com `new`, contornando o DI. O `?` do TypeScript **não é** `@Optional()` do Nest (e `@Optional()` sozinho, sem `@Inject`, também não resolve — o token é `undefined`). Com aquele commit, o `pm2 restart` derrubaria a API **inteira**, não a feature. Pego pelo **DevOps** ao subir o artefato — QA, Security e UX/UI tinham aprovado com 340 testes verdes. Terceira reincidência da classe "build+unit verdes, boot morto" (N-036 feature desligada em silêncio; N-043 enhancer; agora repositório) — a variante desta é **fatal** em vez de silenciosa. Os outros 4 construtores novos da mesma rodada estavam corretos: o defeito era de 3 linhas e derrubava tudo.
**Regra**: (1) todo módulo novo com token Symbol/interface entrega um **spec de fiação** que compila o módulo real (`Test.createTestingModule({ imports:[XModule] }).overrideProvider(PRISMA).useValue(fake)…compile()`) e assere **por parâmetro** que o colaborador foi injetado de fato (não só `service !== undefined` — o caso resolve o repositório e verifica que o parâmetro opcional chegou); o mutante de aceite é **remover o `@Inject`** mantendo o `@Optional()` (deve ficar vermelho — aqui `D-1c`, 1 falha em 7); (2) o par mínimo de toda dependência opcional é `@Optional() @Inject(TOKEN)` — nunca o tipo sozinho; (3) o **aceite de deployability roda no artefato**: `node dist/src/main.js` até `Nest application successfully started` + a linha de anúncio do módulo (`[x] configurado=… sha8=…`) + rotas mapeadas + job registrado, executado por avaliador que **não** escreveu o código (DevOps/QA), com 0 `UnknownDependenciesException`; (4) o Prisma (adapter pg) conecta **preguiçosamente** ⇒ o boot se prova **sem banco/túnel** (conexão recusada em `localhost:5433` não impede o `started`) — não esperar o túnel para provar o D-1; (5) sem `timeout` no host (macOS), `perl -e 'alarm N; exec @ARGV' node dist/src/main.js` encerra o processo — exit **142** (SIGALRM) é a prova de que **morreu**, não de falha; boot de verificação sem isso vira daemon órfão ocupando a porta da próxima prova.
**Origem**: algar, rodada skeelo-ativacao-clientes (25/08/2026) — bloqueante D-1 (`ClausulaSkeeloRepositoryImp` índice `[1]`), it.1 reprovada só pela seção DevOps; fechado na it.2 por `@Optional() @Inject(ParametroSistemaRepository)` + `skeelo.module.di.spec.ts` (7 casos, mutante `D-1c`) + boot reproduzido 3× por 2 avaliadores; L164 do projeto.

### N-048 — Prisma `notIn: []` não exclui NADA: ramo de "fora do conjunto" com conjunto vazio se OMITE do `OR`, o predicado usa a MESMA função do resto do código (nunca SQL/`LIKE` reescrito) e a guarda AVALIA o `where` contra fixtures

**Contexto**: reconciliação/drift em que um job procura linhas cujo item **saiu de uma allowlist** (parâmetro CSV → ids do catálogo) para desfazer um efeito externo **pago** (`unsubscribe`, cancelamento, estorno) — e o contrato promete "retirar o código da lista desassina as linhas vivas na rodada seguinte".
**Problema**: a forma natural `cod_servico: { notIn: idsPermitidos }` está certa com a lista cheia e **catastrófica** com a lista vazia — `notIn: []` é "sem exclusão" ⇒ **toda** linha viva vira drift e o job desfaz a base inteira em lotes até o teto por execução. Segunda armadilha: reescrever o predicado em SQL comparando o `product_code`/snapshot cru (`k.plano NOT IN (…)`) diverge da normalização do código (`trim().toUpperCase()`): `' ebook_x '` seria lido como "fora da lista" ⇒ mesmo efeito. Terceira: um teste que assere a **forma** do `where` (`expect(where.OR[2]).toEqual({ cod_servico: { notIn: ids } })`) passa com o `notIn: []`, e um teste só do service contra repo fake nunca vê o ramo.
**Regra**: (1) conjunto resolvido vazio ⇒ o ramo **não entra** no `OR` (`if (ids.length > 0) or.push(…)`) — allowlist vazia é **inerte**, nunca "tudo fora"; (2) o predicado de pertencimento é **uma função** compartilhada com o caminho síncrono (`servicosSkeeloDoPlano` alimenta tanto o `calcularDiff` do botão quanto o `listarComDrift` do job), aplicada ao catálogo em memória e traduzida em ids — nunca `LIKE`/`NOT IN` sobre a coluna crua; constantes de estado (`ESTADOS_LINHA_VIVA`) exportadas do util e o complemento **derivado**, sem 2ª lista (mexer na constante move o repositório junto — mutante de aceite); (3) a guarda é **comportamental**: o fake do repositório **avalia** o `where` contra linhas-fixture (viva + fora da lista ⇒ entra; viva + na lista ⇒ não entra; lista vazia ⇒ ninguém) e dois mutantes de aceite — remover o `if` e trocar por `notIn: []` — ficam vermelhos; (4) a prévia SQL do runbook repete o predicado do código com `arquivo:linha` (P-062), cobre o conjunto de estados inteiro (vivos = `ATIVO ∪ PENDENTE_ATIVACAO ∪ ERRO_DESATIVACAO`) + `data_exclusao IS NULL`, compara pelo `product_code` **atual** (não pelo snapshot) com coluna `snapshot_divergente`, e declara o que o job **não** cobre (serviço desvinculado do plano só por gancho/botão); (5) toda edição que altere o conjunto (parâmetro **ou** cadastro do serviço — `SERVICO/write`) é gatilho da mesma prévia.
**Origem**: algar, rodada skeelo-ativacao-clientes (25/08/2026) — D-4/D-6 (3º ramo de `listarComDrift`, `clausula-skeelo.repository.ts:372`), guardas D-4a..D-4c do QA, `DEPLOY.md §37.6` reescrito 2×; L165 do projeto.

**Reforço (26/08/2026, algar, integracao-stations-superapp)**: a mesma classe no ramo de **pendentes** do job (não só no drift): a guarda de mapa vazio existia em 2 dos 3 pontos de entrada e faltava justamente no caminho que roda sozinho a cada 30 min — com `{}` (ou parâmetro **ilegível**, degradado para `{}` pelo `catch`) o desejado virava `∅` de negócio ⇒ `DELETE /associate` em lote, para até o teto por execução, sem operador. Guarda **antes** de listar (0 rede, 0 escrita, heartbeat zerado + `DEGRADADO` quando ilegível), nos **três** caminhos, cada um com teste + caso-controle (mapa cheio ⇒ o `DELETE` legítimo acontece); mutante de aceite = remover a guarda de **cada** caminho isoladamente. L169 do projeto.

### N-049 — Repositório com `db: any` + dublê de Prisma: chave inexistente em `select`/`where` passa em build e em 100 % da unit e só falha no BANCO (`PrismaClientValidationError`) — feature inerte com suíte verde; conferir contra o `schema.prisma`, tipar o cliente, nunca presumir coluna por analogia

**Contexto**: repositório Prisma que recebe o cliente como `any` (para aceitar `PrismaClient` ou `Prisma.TransactionClient` sem generics — padrão herdado; L132 do projeto) e specs que substituem o `db` por dublê (`Proxy`/objeto com `jest.fn()`); a rodada seguinte copia o `select`/`where` de outra tabela "parecida".
**Problema**: `where: { data_exclusao: null }` numa tabela **sem** essa coluna e `select: { locale_algar: … }` numa relação que se chama `locale` compilam (o `db` é `any`), passam em 385 testes (o dublê aceita qualquer chave) e **só** contra o banco real lançam `PrismaClientValidationError` — engolida pelo `catch` do service, a feature inteira ficou **inerte com suíte verde** (`itens: []`), descoberta por acaso pelo QA da rodada seguinte ao apontar um E2E para o dump local. A presunção errada ("cláusula/contrato têm `data_exclusao`") viajou para o CONTRACT da feature seguinte (G-7) e para duas queries SQL de censo do runbook, que ninguém executou e que falhariam com `42703`.
**Regra**: (1) todo `select`/`where`/`include` novo é conferido contra o bloco `model` do `schema.prisma` (`awk '/^model X \{/,/^\}/' schema.prisma | grep coluna`) — nunca por analogia com outra tabela nem de memória; (2) o cliente é **tipado** (`PrismaClient | Prisma.TransactionClient`) — com tipo, chave inexistente é `TS2353` no build; com `any`, só o banco valida; (3) feature com repositório novo ganha **1 prova contra banco real** (E2E no dump local ou smoke) antes do aceite — dublê fiel ao produtor (J-003) não substitui isso para **chaves**; (4) SQL de runbook que espelha um predicado do código se confere contra o **DDL** (dump) e roda contra staging com controle positivo (P-062); (5) afirmação de existência de coluna em CONTRACT/runbook leva `arquivo:linha` do schema. Irmã de N-020 (relação não declarada devolve só o id) e N-006 (mock não pega erro de tipo do Prisma).
**Origem**: algar, rodada integracao-stations-superapp (26/08/2026) — 2 bugs de produção da Skeelo achados no Passo 4 (`clausula-skeelo.repository.ts`, commit `539dd08` isolado), M-12 (G-7 do CONTRACT) e D-R1 (`DEPLOY.md §37.1/§38.1` P3); L170 do projeto.

**Reforço (28/08/2026, algar, cadastro-minutas-documento-aceite — 3ª "feature inerte com suíte verde" do projeto)**: `registrarEvidencia` gravava `cod_documento_aceite: undefined` numa coluna **`NOT NULL`** com **3 testes verdes** — o dublê do repositório aceitava qualquer `data` e não expunha o lookup da minuta, certificando uma gravação **impossível**. Fix: ler o snapshot persistido + **ampliar o dublê**; a ampliação só é legítima **provada por mutação** (reverter o dublê ⇒ exatamente 3 vermelhos). Regra adicional: `data` de `create`/`update` capturado do dublê é conferido contra o `schema.prisma` — coluna `NOT NULL` sem default ⇒ asserção de presença e tipo no teste.

### N-050 — URL vinda de parceiro (link, callback, download temporário) passa por allowlist de host EXATO antes de `fetch`, persistência, e-mail e push — e allowlist fail-closed é um sintoma de plantão com deploy como remédio

**Contexto**: integração em que o parceiro devolve URLs (link de assinatura, URL temporária de download em CDN/S3, endereço de callback) que o backend consome (`fetch`), persiste, manda por e-mail ou empurra a outro sistema; o projeto já tem uma allowlist de hosts do parceiro usada só no front.
**Problema**: A-2 e A-3 eram **o mesmo defeito em 2 caminhos**: o `/download` seguia a URL crua devolvida pela D4Sign (SSRF: um parceiro comprometido ou um 302 levaria a credencial do servidor a host arbitrário) e o link de assinatura ia para banco, e-mail e push sem validar o host (phishing com aparência oficial). A allowlist existia no repositório (`HOSTS_EVIDENCIA_PERMITIDOS`) e **nenhum caminho do backend a usava**. A correção cria um segundo efeito: allowlist é fail-closed ⇒ se o parceiro trocar o domínio dos links (CDN nova), a feature para com `causa=host-nao-permitido` e 0 rede — comportamento correto, mas é um sintoma de **plantão** cujo remédio exige **deploy** (não há parâmetro de banco), e a tentação de ampliar com um guarda-chuva (`amazonaws.com`) reabre o SSRF.
**Regra**: (1) toda URL recebida de terceiro passa por `hostPermitido(url)` — `https` obrigatório, sem `user:pass@`, host **exato** de uma lista versionada — **antes** de qualquer uso (fetch, persistência, template de e-mail, payload de push), num único util com testes de hosts hostis (sufixo, prefixo, `@`, `http`, IP, porta, unicode) + controles positivos e prova de "`fetch` não chamado"; (2) `redirect:'error'` em todo `fetch` a parceiro e download por **stream** com teto de bytes que **recusa** (não trunca) — e a rota que serve o arquivo ao cliente **nunca** responde 302 para o parceiro (o `Authorization: Bearer` do operador iria junto); (3) a allowlist ganha slug de diagnóstico próprio, item de runbook "o que fazer quando aparecer" e item de **smoke** (a URL temporária de download é a que mais muda); (4) ampliação é sempre com o host **exato observado**, nunca sufixo — e é mudança de código com deploy, declarada como tal no runbook; (5) método morto que devolve a URL crua "para uso futuro" é a porta de reabertura — apaga-se na mesma correção (P-067).
**Origem**: algar, rodada integracao-d4sign (27/08/2026) — A-2/A-3 (Security, it.1 🟠), `d4sign-url.util.ts` + `B-015`/`B-016c`, N-1/N-2 da 2b; risco 8 do EVALUATION; L175 do projeto.

### N-051 — Token `Symbol` num spec que mistura `import` no topo com `require()` no caso: dois carregamentos do módulo = dois Symbols, e `Nest could not find Symbol(X)` INTERMITENTE sob os workers do Jest — forma de carga única, compile memoizado e asserção de identidade por `Reflect.getMetadata`

**Contexto**: spec de fiação (N-036/N-047) que compila **módulos reais** do Nest (`Test.createTestingModule({ imports:[Hospedeiro] })`) e resolve tokens `Symbol('XRepository')` exportados por arquivo de domínio; suíte completa grande rodando em paralelo (`maxWorkers` default), às vezes com outra bateria concorrente na mesma máquina.
**Problema**: o spec importava o `D4SignModule`/tokens no topo (ESM) e fazia `require()` dos hospedeiros dentro dos casos ⇒ sob certos caminhos o registry de módulos do Jest entregava **duas instâncias** do arquivo do token, cada uma com o seu `Symbol()`: o container registrava um, o `moduleRef.get()` pedia o outro ⇒ `Nest could not find Symbol(DocumentoAssinaturaRepository) element` em **2 de 3** execuções paralelas — e a suíte que caía era exatamente a **guarda do bloqueante** da iteração anterior (guarda instável é guarda fraca). Agravante de diagnóstico: com 2 `jest` completos concorrentes (avaliadores em paralelo), suítes **alheias** caem por `Exceeded timeout of 5000 ms` e o "flake" parece espalhado; o QA chegou a eleger `--runInBand` como número oficial.
**Regra**: (1) spec que compila grafos reais usa **uma** forma de carga (tudo `import` estático no topo; `require()` só se for a única forma, e então em todos os pontos) — `Symbol` é identidade por **referência**; (2) compilação **memoizada** por hospedeiro (`Map<Módulo, Promise<TestingModule>>`) e `afterAll` fechando todos os containers (nada vaza para o worker); (3) 1 caso assere a **identidade** do token: o `Symbol` importado pelo spec `===` o que o módulo registra em `providers` (`Reflect.getMetadata('providers', Modulo)`), para que a divergência, se voltar, se explique sozinha; (4) o número oficial de regressão é a suíte **paralela padrão medida sem carga concorrente** — `--runInBand` serve para confirmar delta, não para substituir a medição; baterias completas de avaliadores diferentes se **serializam** (J-012); (5) "flake" só vira achado com o par isolado ×3 **e** paralelo sem carga.
**Origem**: algar, rodada integracao-d4sign (27/08/2026) — QA-03 (it.2) → fix 2b em `d4sign-hospedeiros.spec.ts:56-68,136-159,187-201` (B-085l), 3× 10/10 isolado + 4× suíte completa paralela verde; L178 do projeto.

### N-052 — Agregar relação por `include`/`select` traz ao processo TODAS as colunas da relação — inclusive credencial (`link_assinatura`) e PII — só para contar: agregação é `count`/`groupBy` por id, e o repositório da tela ganha teste de LISTA NEGATIVA de relações

**Contexto**: tela administrativa de um cadastro (minutas) que mostra "quantas evidências/filhos" por linha, com relações que carregam **segredo** (link de assinatura = credencial que abre o documento) ou **PII** (IP do aceite, user-agent, token de validação), sob um perfil que só tem `read` da tabela pai.
**Problema**: o caminho natural (`include: { documento_assinatura: true }` + `.length`) lê as colunas sensíveis para a memória do processo e para qualquer log/serialização acidental; a projeção por whitelist (17 colunas) da tabela pai fica inútil se a relação entra inteira. O Security classificou como CRÍTICA **se ocorresse** e a rodada contratou a proibição antes do código existir.
**Regra**: (1) contagem de relação = `groupBy`/`count` por chave estrangeira (2 consultas, sem N+1), **nunca** `include`/`select` da relação; (2) o repositório da feature tem teste que assere a **ausência** de `include`/`select` das relações nomeadas (lista negativa) + whitelist positiva de colunas do pai; (3) `_count` do Prisma só quando nenhuma relação com segredo entra no mesmo `select`; (4) o Security nomeia no CONTRACT quais relações são "só contáveis" e o QA escreve a lista negativa como teste no Passo 4.
**Origem**: algar, rodada cadastro-minutas-documento-aceite (28/08/2026) — C-2/SEC-1410 → SEC-1459; L200 do projeto (par de L181, bit `execute` do link).
